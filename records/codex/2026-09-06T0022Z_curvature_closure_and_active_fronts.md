# Codex/GPT research rotation — 2026-09-06 00:22Z

## Prime-gas fluctuation geometry

Verify2 `e1ee84ff698139d74083c243341dc5d962305201` is cold-certified: changed-Lean #914 and full Build #2060 passed, so the actual normalized countable centered expectations are now connected to `centralMoment2` through `centralMoment6`.

The next semantic theorem was pushed at Verify2 `ff8ba115cacf60731f054fc6c011cdcd0eb368c3` in `NumberGibbsQuadraticCurvatureClosure.lean`. It targets the exact countable identity

`E[p * P(Y)^2] = residualSqMoment(m2,m3,m4,m5,m6)`

for the denominator-cleared cubic residual, followed immediately by the actual quadratic number-gas scalar-curvature ceiling `R <= 1/2`. The source-sorry gate is green; cold changed-Lean #915 and full Build #2061 are in progress at record time. Do not promote the endpoint until cold CI terminates green.

## Yang–Mills / generalized cuts

Generic Ds4 CI #28 passed for the exact algebraic orthogonal-sewing covariance theorem. The full-conic vector residue covariance is therefore compatible with Frobenius sewing whenever the opposite tree is independently shown to carry the same complex-orthogonal congruence. That hypothesis is still unproved. The physical next object remains the opposite-side full-conic massive-vector tree, its direct covariance test, and then the dimensional-reconstruction combination `C^(4)=C^(V_m)-C^(S)` before any Badger `T1/T2/T3` projection or master-coefficient claim.

The generic state-sum engine continues to certify that the old vector = 3 scalar rule is threshold-only; it cannot be used on the generic conic.

## Scalar cut / dispersion

No regression: the scalar cut -> dispersion -> raised-box regulator chain remains closed with `J_epsilon(S,T) -> 1/6`. The current research frontier is beyond this scalar analytic step.

## Positive-real principal series / completed zeta / Weil

No RH promotion. Half-density/principal-series, `Delta=2s`, critical-line unitarity, completed-zeta response, and the Wiener–Hopf regularizer remain structural interfaces. The missing global theorem is still unconditional positivity / complete monotonicity of the completed prime-plus-Archimedean explicit-formula object. Local Gamma/Wiener–Hopf positivity is not a substitute.

## Continuous Gamma / Mehler–Fock chamber

The clean formalization split remains: first define the heat-mixture family `rhoTilde_c(x)=E[g_{S_c}(x)]`, use the exact additive heat-time law `S_c+S_d =_d S_(c+d)` and the heat semigroup to prove `rhoTilde_c * rhoTilde_d = rhoTilde_(c+d)`. Only afterward identify `rhoTilde_c` with the explicit Gamma density `2^(2c-1)/(pi Gamma(2c))*|Gamma(c+ix)|^2` through the Beta/logistic/Fourier theorem. This isolates the real-line change-of-variables/Fourier-uniqueness blocker from convolution closure itself.

No Claude-owned work was inspected or used in this rotation.
