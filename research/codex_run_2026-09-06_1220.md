# Codex/GPT research rotation — 2026-09-06 12:20 EDT

Codex/GPT track only. No Claude-owned work inspected.

## Verify2 CI repair

Cold changed-Lean #934 failed on `29a10a56de8b76ca9769a75c365a1b35eabf387f` while full Build #2080 passed. The exact cold error was syntactic: `(lt_div_iff₀ hden).mp h` produced

`1 * (c * (2*c+1)) < 2*(c^2+x^2)`

while the annotated intermediate expected the proposition after simplifying `1 * denominator`. The theorem statement and mathematics were unchanged. Verify2 repair `301110d9f3f8208325658b35c984d6c4d9ad8fdd` changes that step to `by simpa using ...`. Cold #935 and full Build #2081 are running.

The continuous chamber statements remain

- `F_c(x)=2(c^2+x^2)/(c(2c+1))`, `c>0`;
- `F_c(x)>0`;
- `F_c(x)=1 iff 2x^2=c`;
- `F_c(x)>1 iff c<2x^2`;
- `F_c(x)<1 iff 2x^2<c`;
- `F_{k+1}(x)=rhoStepFactor(k,x)`.

No cold certification is claimed until #935 is terminal green.

## Active-front boundaries

- Scalar celestial cut/dispersion/raised-box regulator remains closed with `J_epsilon(S,T) -> 1/6`.
- YM baseline remains pre-master. Physical q-to-propagator normalization is green; the missing object is the factor-preserving opposite crossed full-conic tree with extra uncut denominators retained through topology subtraction before `C^(4)=C^(V_m)-C^(S)` and Badger extraction.
- Principal-series / completed-zeta / Weil: `Delta=2s`, critical-line unitary normalization and local Gamma/Wiener-Hopf structure remain exact; unconditional positivity of the completed prime-plus-Archimedean explicit-formula/Weil form remains the RH-critical blocker.
- Prime-gas: strongest certified countable endpoint remains `R(beta,eta)<1/2` for `eta>0`.
- Spectral/chamber: after cold certification, next formal targets are covariance/moment transport, radial chamber monotonicity, and the arbitrary-c heat-mixture convolution semigroup; normalized Gamma-density identification remains a separate analytic layer.
