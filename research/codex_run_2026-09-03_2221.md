# Codex/GPT all-fronts continuation — 2026-09-03 22:21 EDT

## Scalar regulator / celestial cut front

Cold changed-module CI #869 failed before the final raised-box theorem was compiled. The failure was an invalid import in `RaisedBoxConcreteVolumeClosure.lean`: `Mathlib.MeasureTheory.Integral.IntervalIntegral.IntegralEqImproper` does not exist in the pinned Mathlib revision. Repository/API audit located `intervalIntegral.integral_pow` in `Mathlib.Analysis.SpecialFunctions.Integrals.Basic`. Verify2 commit `63ccc362eb3547865b805711952d8a2bca5989be` replaces only that import; the polynomial proof and the intended normalization `simplexVolume = 1/6` are unchanged. Changed-Lean #870 and Build #2016 are running on the repaired source. Therefore the final theorem `simplexMoment ε S T -> 1/6` remains candidate until the cold changed-module run is terminal green.

## Prime-gas thermodynamics

The certified countable identities remain `∂β Z = -M1`, `∂η Z = -M2`, and the corresponding Massieu gradient identities. Source audit of `NumberGibbsQuadraticPartitionDerivatives.lean` confirms the usable local pattern for the next layer: `hasDerivAt_tsum_of_isPreconnected` plus the global quadratic-confinement envelope. The next exact countable moment derivatives are

- `∂β M1 = -M2`, `∂η M1 = -M3`;
- `∂β M2 = -M3`, `∂η M2 = -M4`.

These imply the covariance Hessian
`∂ββ log Z = M2/Z - (M1/Z)^2`,
`∂βη log Z = M3/Z - M1*M2/Z^2`,
`∂ηη log Z = M4/Z - (M2/Z)^2`.
No new Lean push was made while scalar cold CI is active, to avoid superseding the required direct certification job.

## Principal series / completed zeta / Weil

No RH promotion. Local half-density/principal-series unitarity, completed-zeta response, heat/Poisson bridges and finite interpolation remain structural inputs. The unresolved global theorem remains positivity of the actual completed prime-plus-Archimedean Weil quadratic form on a concrete admissible transform class, together with the required finite pair-support interpolation/closure for that same class.

## Spectral / Mehler-Fock / Wiener-Hopf

Focused-source audit re-confirms a terminology correction: `P(λ)=πλ/sinh(πλ)` is the derived Gamma/phase-space/Wiener-Hopf spectral factor, not the representation-theoretic `SL(2,C)` Plancherel density. Integer Gamma/WH and corrected kinematic ODE results remain certified. The continuous chamber law remains discovery-level pending arbitrary-`c>0` Fourier-Gamma/Barnes transform and Fourier uniqueness.

## Yang-Mills / gravity

No numerator was guessed. The honest downstream gate remains the complete `D_s=4`, `μ≠0` two-massive-vector color-ordered tree, double physical-projector sewing, and exact color/coupling/orientation/scalar-subtraction/FDH normalization before generalized cuts, higher loops, or gravity double copy are promoted.

No Claude-owned branch, notes, files, records, or workspace were inspected.
