# Codex/GPT research rotation — 2026-09-03 12:20 America/Toronto

Scope: Codex/GPT track only. No Claude branch, files, notes, records, or workspace inspected.

## 1. Celestial cut / scalar regulator

Certified baseline `2d59730c6e6e7bbb7be36778a572593ddac83a44`: changed-Lean #845 and Build #1991 passed. This certifies the full physical fixed-x1 nested bridge

`fullSimplexFiberIntegral_eq_nestedIntervals_of_physical_bounds`

and the supporting product-integrability/Fubini chain.

New Verify2 module `RaisedBoxOuterDCTMajorant.lean`, commit `0db4473a19d78acc5f3d74f11df3f9e8800a936c`, packages the final outer majorant:

- `(S*x)^(-delta)` is interval-integrable on `[0,1]` for `delta<1`, `S>=0`;
- hence `1 + (S*x)^(-delta)/(1-delta)` is interval-integrable for `0<delta<1`, `S>0`.

Changed-Lean #846 passed. Full Build #1992 was cancelled only because later workbench pushes superseded it, not because of a source failure.

The remaining scalar theorem is the outer DCT assembly: use strong measurability of the full product fiber, the certified nested bridge on the physical interval, `middle_interval_tendsto_inner_one`, and this integrable majorant. The target is `simplexMoment(epsilon,S,T) -> simplexVolume = 1/6`.

The focused loops-from-cuts paper remains correctly bounded: bubble and internally regulated scalar box are the proved examples; the `(4+2L)`-tree to `L`-loop pattern is an organizing principle, not yet an all-topology theorem.

## 2. Prime-gas thermodynamics

Discovery2 executable `discovery/prime_gas_global_derivative_envelope.py`, commit `f45100ce21cf8ba13b922d72d85855dc6159a768`, found a stronger route than the earlier eventual tail bound. Completing the square gives, for `L>=0`, `beta>=-B`, `eta>=eta0>0`,

`-beta L - eta L^2 <= -2L + (B+2)^2/(4 eta0)`.

Therefore for every fixed moment order `r` and every index `n`,

`w_{beta,eta}(n) L_n^r <= exp((B+2)^2/(4 eta0)) w_2(n) L_n^r`.

This is a single all-index summable compact-parameter majorant and eliminates the finite-exception splice in the planned `hasDerivAt_tsum_of_isPreconnected` proof.

Verify2 `NumberGibbsQuadraticGlobalEnvelope.lean` encoded the exact bound. First direct smoke #847 rejected only the final packaging line because pinned Mathlib v4.19.0 has no `Summable.const_mul`; the three substantive inequalities compiled. Commit `7ba029a55cf0069306bc5f16e1138976694faa45` replaced it by `Summable.mul_left`; changed-Lean #848 passed.

Thus the remaining thermodynamic gap is now genuinely termwise differentiation of the countable partition sum. Local first/second summand derivatives and the all-index summable envelopes are both present. Once interchanged, the Hessian is the covariance matrix of `(L,L^2)`, and the already-certified strict Fisher determinant gives strict convexity/local invertibility.

## 3. Positive-real principal series / completed zeta / Weil

No RH claim promoted. The exact formal interpolation reduction is sharper than global surjectivity: Lagrange interpolation plus one admissible seed transform nonzero on each finite pair-support and closure under polynomial spectral multipliers is enough to realize arbitrary finite coefficient data. The remaining analytic obligations are still:

1. positivity of the actual completed prime-plus-Archimedean Weil quadratic form on one concrete admissible class;
2. nonvanishing-seed + polynomial-multiplier closure for that same class on every finite pair-support.

The local half-density dictionary `Delta=2s`, critical-line/principal-series unitarity, and completed-zeta phase response do not by themselves discharge (1).

## 4. Spectral / Mehler-Fock / Wiener-Hopf / chambers

Focused-source audit isolated a sign typo in the explanatory direct check of the kinematic Fourier-partner ODE. The stated ODE itself is correct:

`(1/2) sinh(x) pHat'(x) = pHat(x) - pHat(0)`,

with `pHat(x)=1/(4 cosh(x/2)^2)` and `pHat(0)=1/4`. Since

`pHat'(x)=-(1/4) sech^2(x/2) tanh(x/2)`,

the left side is `-(1/4)tanh^2(x/2)`, not the positive sign written in the prose; this equals `pHat(x)-pHat(0)` exactly.

Verify2 candidate `KinematicWeightShiftODE.lean`, commit `081611124299f07aa9c788aab6c030554d1fc0d2`, now encodes `pHat`, its sech-squared identity, its exact derivative, and the ODE. Certification was pending at the close of this record.

The integer Gamma/Wiener-Hopf chamber hierarchy remains certified. The continuous `rho_c` convolution semigroup and Levy law remain discovery-level until the arbitrary-c Barnes/Fourier-Gamma transform plus Fourier uniqueness are formalized.

## 5. Yang-Mills / gravity

No numerator was reverse-engineered or promoted. The existing same/mixed-helicity state-sum identities are exact algebra but explicitly condition amplitude interpretation on a genuine sewing identification. The honest gate remains the full nonzero-mu `D_s=4` color-ordered two-massive-vector tree current, both physical projectors, and coupling/color/cut normalization. Generalized cuts, higher loops, and gravity double copy stay downstream.

## Repository state

The certified workbench cannot currently be fast-forwarded to main without reconciliation: main and `codex/lean-workbench` have diverged, with main carrying commits absent from workbench. No force update was attempted.

## Next frontier

1. certify/repair `KinematicWeightShiftODE.lean`;
2. assemble the final raised-box outer DCT and conclude `simplexMoment -> 1/6`;
3. instantiate the global prime-gas envelope in Mathlib's countable derivative theorem and obtain derivatives of `Z(beta,eta)`;
4. after scalar closure, move directly to the honest nonzero-mu Yang-Mills sewing calculation.
