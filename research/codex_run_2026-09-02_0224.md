# Codex/GPT research run — 2026-09-02 02:24 EDT

Codex/GPT track only. Claude material was not inspected.

## Re-anchor

- Read `website/CODEX.md` and followed the discovery-first/no-sorry/no-open-axiom discipline.
- `GPPVerify2:codex/lean-workbench` began at `aa276b9ae4b20e665f46abaebe240e1fcd2c833b` and was advanced this run.
- `GPPDiscovery2:codex/discovery-workbench` began at `5889d7f037006d7bd2b4eaea23fc6646c474a314`.
- Supabase `codex.research_notes` read was attempted and blocked by the connector safety boundary, so this repository note is the fallback durable run record; no database state was guessed or fabricated.
- Focused source mining used the uploaded scalar-box paper and arithmetic principal-series/RH program.

## A. Celestial cut / scalar-box regulator

The current inner DCT still carried an explicit eventual `AEStronglyMeasurable` hypothesis for the concrete integrand `Q(S,T,x1,x2,x3)^(-ε)`.

Mathlib already supplies measurable real powers (`Measurable.pow_const`). Since `Q` is polynomial/affine in `x3`, the concrete integrand is Borel measurable for every fixed parameter tuple. Added:

`GppRaisedBoxConcreteMoment.integrand_measurable_x3`

and used it to remove the measurability hypothesis from:

`GppRaisedBoxInnerDCT.inner_interval_tendsto_one`.

Commits:
- `d96ee4f3bf287f486c087c6ecddfd4f520c03abf` — formalize raised-box integrand measurability.
- `75d82a1b2f734a7e393e1ba58c9a33b3404fd69f` — discharge raised-box inner DCT measurability.

This makes the first regulator-removal-through-integration theorem unconditional on its stated physical hypotheses (`0<δ<1`, `S,T>0`, strict interior `x1,x2`). It does not yet prove the full three-simplex limit `J_ε→1/6`; the next analytic frontier is the middle/outer Fubini-DCT chain.

Source audit: the focused scalar-box manuscript correctly limits the general higher-loop statement to an organizing principle; it does not prove arbitrary topology/generalized-cut completeness. Keep that distinction in Verify2.

## B. Positive-real / completed zeta / Weil

Source mining of `arithmetic_principal_series_RH_program-34.tex` reconfirmed the true global wall: the local half-density/principal-series unitarity and spectral weight `P(λ)=πλ/sinh(πλ)` are useful exact structure but are not the missing RH theorem. The sharper global object is the zero-independent prime–Archimedean heat/Weil form. The paper's exact candidate is

`m_*(u)=∫_0^∞ exp(-(1+u)t) K(t) dt`

with `K(t)` formed from the completed boundary distribution; RH is formulated there as complete monotonicity / additive-semigroup positivity of `K(t_i+t_j)`. This remains a source target to audit and formalize without importing any zero-dependent representation into the forward direction.

No RH claim promoted this run.

## C. Prime-gas thermodynamics

Audited `NumberGibbsQuadraticThermodynamics.lean`. The current exact package already has global-in-β partition summability for every `η>0`, strict positivity of `Z`, and exact entropy/free-energy Legendre identities. The next substantive thermodynamic step should not be another algebraic rearrangement: it is differentiation of `log Z(β,η)` and identification of the Hessian with the covariance/Fisher matrix under the existing quadratic-confinement summability, followed by genuine two-parameter curvature if the differentiability layer closes.

No new prime-gas theorem was pushed this run because the scalar and spectral closures were higher-value exact targets.

## D. Gamma / Mehler-Fock / Wiener-Hopf chamber structure

From the already-certified adjacent recurrence, derived a new exact chamber-selection theorem. For `k>0`, chamber `k` is a strict local maximum of the real Gamma spectral density iff

`k < 2*x^2 < k+1`.

Added:

`GppWienerHopfGammaChamberMonotonicity.rhoGamma_re_strict_local_max_iff`

at commit `ec6ac9cafaab1145c5dfc767f461a00dbefc3847`.

No convolution hypothesis is used; this is a direct consequence of the exact positive recurrence factor and adjacent crossing theorems.

## CI / integration

Opened draft certification PR `GPPVerify2#22` solely to run the full repository gates on the current Codex workbench. Exact current head: `ec6ac9cafaab1145c5dfc767f461a00dbefc3847`.

Build run `33598935904` / Build #1935 is pending at the time of this record. Do not call the new theorems certified until that exact head is green.

The workbench is currently diverged from `main` (ahead 60, behind 3). Do not merge merely to synchronize; reconcile only after exact-head CI and preserve the three newer main commits.

## Next frontiers

1. Repair any exact-head CI failure first, then continue scalar-box middle/outer DCT toward `simplexMoment ε S T → 1/6`.
2. Formalize the prime–Archimedean heat kernel definition from the explicit completed distribution in a zero-independent form; separate the explicit-formula identity from the positivity criterion.
3. Add the differentiability/Hessian layer for the quadratically confined number-Gibbs family and identify `∂² log Z` with the two-parameter covariance matrix.
4. Extend the Gamma chamber theorem from local maxima to a global unimodality/argmax statement with exact tie cases at `2x²∈ℕ`, if the finite/infinite index bookkeeping remains clean.
5. After scalar closure, return to honest `μ≠0`, fixed-loop-momentum Yang–Mills state sums and generalized cuts; do not infer the full amplitude from two-particle cut matching alone.
