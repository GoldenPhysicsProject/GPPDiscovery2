# Codex/GPT research run — 2026-09-03

Codex/GPT track only. No Claude branch, records, or workspace were opened or used. An unrelated file-library search returned an automatically generated Claude-titled search hit; it was ignored and not opened.

## Scalar raised-box regulator

The prior physical Fubini closure baseline `9fea474dd0f60b347067f24a76b8ea97e96c2698` is certified by changed-Lean smoke #841 and Build #1987.

This run added `GppVerify/CelestialHolography/RaisedBoxOuterNestedBridge.lean`. The bridge first composes certified two-dimensional fiber integrability/Fubini with `stripInnerIntegral_eq_intervalIntegral`, eliminating the measurable inner strip. It then removes the outer `Icc` indicator via the null-endpoint identity and oriented interval integration. Candidate endpoint:

`fullSimplexFiberIntegral_eq_nestedIntervals_of_physical_bounds`

under `0 < delta < 1`, `0 <= epsilon <= delta`, `S,T>0`, and `0<x1<1`. Its right side is exactly the original nested `x2`/`x3` interval coordinate expression occurring inside `simplexMoment`.

Changed-Lean smoke #844 on `22dfc661cdd84164848cb82a36d0ae9c31d666c9` failed before reaching the new theorem, while rebuilding `RaisedBoxMiddleMeasurability.lean`. The latent defect was exact: `intervalIntegral.integral_nonneg_of_forall` requires global pointwise nonnegativity, but the one-channel majorant is only being used on the physical interval and the proof attempted to infer `x3 >= 0` without an interval-membership hypothesis. No mathematical scalar claim was retracted. The repair replaces the global theorem by `intervalIntegral.integral_nonneg_of_ae_restrict`, obtains `x3 in Icc 0 L` almost everywhere from the restricted measure, and then proves nonnegativity of `1+(S*x1*x3)^(-delta)` on that interval. Repair commit: `2d59730c6e6e7bbb7be36778a572593ddac83a44`. Changed-Lean smoke #845 and Build #1991 are pending at the time of this record.

If green, the two-dimensional product-measure bookkeeping is finished. Remaining raised-box work is the outer `x1` dominated-convergence assembly and identification with `simplexMoment`, yielding the exact regulator limit `simplexMoment epsilon S T -> 1/6`.

## Prime-gas two-parameter thermodynamics

`NumberGibbsQuadraticTermDerivatives.lean` now contains exact first and second summand derivative candidates. For `L_n = log(n+1)` and `w = exp(-beta L_n - eta L_n^2)`, the local Hessian factors are

- beta-beta: `L_n^2 w`,
- beta-eta: `L_n^3 w`,
- eta-eta: `L_n^4 w`.

The first derivatives `-L_n w` and `-L_n^2 w` were already certified. The second-derivative commit `9b76997ddae4a29917e21646681cafe313d2a38b` passed changed-Lean smoke #843; its full Build #1989 was cancelled only because later workbench commits superseded it. Quadratic confinement already provides a common exponent-2 summable log-moment envelope, so the remaining analytic problem is precisely `tsum` differentiation/interchange on a parameter neighborhood. The repository's existing `hasDerivAt_tsum_of_isPreconnected` use in the regulated-dilogarithm formalization supplies a concrete Mathlib pattern, but the number-Gibbs proof still needs an all-index summable derivative majorant assembled from the eventual uniform tail plus finitely many initial terms.

## Principal-series / completed-zeta / Weil

No RH promotion. `WeilInterpolationBridge.lean` already proves global transform surjectivity is unnecessary: positivity on a concrete test-transform class plus arbitrary finite interpolation only on `S union iota(S)` implies the finite Weil criterion and hence RH. The unresolved analytic work remains both (i) positivity of the genuine completed prime-plus-Archimedean explicit-formula form for that class and (ii) finite pair-support interpolation. Local Gamma/Mehler-Fock/Wiener-Hopf positivity does not discharge (i).

## Spectral / Mehler-Fock / Wiener-Hopf / chambers

The exact all-real formalized spectral bridge remains

`wienerHopfWeight(lambda) * mehlerFockWeight(lambda) = lambda^2 Gamma(1/2+i lambda) Gamma(1/2-i lambda) = pi lambda^2/cosh(pi lambda)`

with the removable origin handled in Lean. The continuous Gamma-chamber convolution/characteristic law remains discovery-level because its arbitrary-positive-parameter Barnes/Fourier-Gamma transform and Fourier uniqueness are not yet formalized. No low-order convolution extrapolation was promoted as a theorem.

## Yang-Mills / gravity

No dynamic sewing numerator was claimed. `MassiveVectorGenericDefect.lean` certifies the exact rational/physical state-sum algebra and mixed-helicity dimensional reconstruction, but this is not a derivation of the full color-ordered two-massive-vector tree current. The amplitude gate remains fixed-loop-momentum `D_s=4`, nonzero-mu tree sewing with both physical projectors, coupling/color/orientation and cut normalization intact; generalized cuts, higher loops and gravity double copy remain downstream.
