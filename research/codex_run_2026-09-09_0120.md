# Codex/GPT research run — 2026-09-09 01:20 EDT

## Verified repository state

- GPPVerify2 `codex/lean-workbench` began at `fcfb5416e5d0d5c46f431b8cd7f5c4443a3381c6`.
- Full Build #2132 completed successfully on that exact head.
- Changed-Lean smoke #984 completed successfully on that exact head.
- GPPDiscovery2 `codex/discovery-workbench` began at `ccc268f81b8e2e6989792228849e778f19bebc26`.
- `main` remains genuinely diverged from the Codex workbench: at the checked boundary the workbench was 254 commits ahead and 7 behind. Do not force-promote over the seven main-side commits; reconcile them first.

## New formal result pushed

Updated `GppVerify/QuantumGravity/OddModeLevyKernel.lean` on GPPVerify2 and pushed commit
`134f725b25daf5df479885161b37addf968106c6`.

For `t > 0`, `a : R`, and `N : N`, the new theorem targets the exact shifted odd-mode tail

    sum_{n>=0} 2 a exp(-(2(N+n)+1)t)
      = (a / sinh t) exp(-2Nt).

At chamber scale `t = pi y`, `y > 0`, this gives

    sum_{n>=0} 2 a exp(-(2(N+n)+1) pi y)
      = (a / sinh(pi y)) exp(-2 pi N y).

This is exactly the finite-to-infinite tail factor discovered numerically/analytically in the preceding run. It sharpens the odd-mode -> continuous Levy kernel bridge by making the relative truncation factor explicit rather than merely proving the infinite sum.

CI triggered for the new head:
- Build #2133: in progress at the time of this note.
- changed-Lean #985: in progress at the time of this note.

Do not call this new theorem cold-certified until both lanes terminate green.

## Active mathematical boundaries

### Chamber / Wiener-Hopf

Already cold-certified before the new tail commit:

    sum_{n>=0} 2 a exp(-(2n+1) pi y) = a / sinh(pi y), y>0,

hence pointwise Levy numerator and density

    nu_a(y) = a / (y sinh(pi y)), y>0.

The new tail theorem is the next exact quantitative layer. Downstream formal targets are compensated Levy integrability, the Levy-Khintchine exponent, semigroup closure, and finally the separate Fourier-uniqueness identification with the explicit Gamma-square density.

### Celestial / YM / gravity

No unsupported coefficient promotion. Scalar celestial cut -> dispersion -> raised-box/regulator chain remains closed with endpoint `J_epsilon -> 1/6` in its proved scope. Generic YM remains blocked at the convention-locked opposite crossed physical tree on the full triple-cut conic; existing helicity-phase/Frobenius invariants are pre-sewing data and do not by themselves identify the physical second tree. After that: `D_s=4` vector-minus-scalar sewing and Badger `T1/T2/T3`; generic gravity and generalized higher-loop cuts are downstream.

### Principal series / completed zeta / Weil

Analytic principal-series fourth-moment closure remains

    A4 = 3/8 - log 2 + 2 log^2 2 - pi^2/24.

Honest Lean formalization still requires Barnes first lemma plus parameter differentiation. No RH promotion: completed prime-plus-Archimedean Weil positivity / global Schur gluing remains the arithmetic obstruction.

### Prime-gas thermodynamics

Certified confined two-parameter entropy results remain:

    beta d_beta S + eta d_eta S < 0,
    grad S != 0  (eta > 0),

with scalar-curvature conjecture `R <= 0` still rejected and global certified envelope `R < 1/2` retained.

## Record-system note

The Supabase connector again blocked direct reads of `codex.research_notes` / `codex.formalization_queue` at the safety layer during bootstrap. This GitHub run note is a durable fallback record, but the canonical `codex.*` ledger should be updated when the connector permits it.

No Claude-owned branches, notes, or context were inspected.
