# Codex/GPT research rotation — 2026-09-03 18:21 EDT

Codex/GPT track only. No Claude branch, notes, files, records, or workspace inspected.

## Certified baseline

- `GPPVerify2:codex/lean-workbench` head `df3a2a368f04ee91b116fedc3380633f495a0183` passed changed-Lean #859 and full Build #2005.
- Therefore both countable partition derivatives are certified for `η > 0`:
  - `∂β Z = -M1`;
  - `∂η Z = -M2`.

## New formal candidate

Pushed Verify2 `3a8dca4dd9ae04a135b846f298bf7baccaca7e1e`, adding `NumberGibbsQuadraticMassieuDerivatives.lean` with the exact normalized gradient laws

`∂β log Z = -internalEnergy`,
`∂η log Z = -quadraticEnergy`.

These use only the certified partition derivatives, `Z_pos`, and the derivative of `Real.log`. Changed-Lean #860 and Build #2006 were in progress at the end of this rotation; do not report these theorems as certified until terminal-green.

## Scalar box

No new obstruction. The final theorem remains the outer `x1` dominated-convergence assembly. Certified inputs already include nested measurability on the strict physical interval, middle-coordinate convergence, the norm bound by `1 + (S*x1)^(-δ)/(1-δ)`, integrability of that majorant on `[0,1]`, and `simplexMoment 0 S T = simplexVolume`. The remaining Lean bookkeeping is transfer across the null endpoint `x1=1` plus the final interval-integral DCT invocation.

## Spectral/source audit

Drive source `blackbody_law_qg_dtoupin_v1.pdf` still calls `P(λ)=πλ/sinh(πλ)` a Plancherel weight. Preserve the current correction: this is the derived Gamma/phase-space/Wiener-Hopf spectral factor and must not be identified with the genuine `SL(2,C)` Plancherel measure. `qg_kinematic_dtoupin_v1.pdf` is useful source material for the exact Mehler-Fock/shadow decomposition, but representation-theoretic and loop phase-space weights must remain separated.

## Weil / RH boundary

No RH promotion. The global missing theorem remains positivity of the genuine completed prime-plus-Archimedean Weil quadratic form on one concrete admissible transform class, together with finite pair-support interpolation for the same class. Local half-density unitarity, Gamma/chamber positivity, or transform identities do not establish this.

## YM / gravity boundary

No numerator was guessed. The honest next dynamical gate remains the complete `D_s=4`, nonzero-`μ` color-ordered two-massive-vector tree and double physical-projector sewing with color, coupling, orientation, scalar subtraction, and FDH normalization intact.

## Next

1. Terminal-check #860/#2006 and repair if necessary.
2. If green, differentiate the first/second moments through `tsum` to obtain the covariance Hessian of `log Z`.
3. Push the final scalar outer DCT to `simplexMoment ε S T -> 1/6`.
4. Continue source mining and keep the Plancherel/Gamma-weight distinction explicit.
