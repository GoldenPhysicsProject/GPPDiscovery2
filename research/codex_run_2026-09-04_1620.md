# Codex/GPT all-fronts continuation — 2026-09-04 16:20 EDT

Scope: Codex/GPT work only. Claude-owned work was not inspected.

## Prime-gas fluctuation geometry

The countable moment derivatives remain cold-certified:

`∂β M1=-M2`, `∂η M1=-M3`, `∂β M2=-M3`, `∂η M2=-M4`.

Cold #890 failed in the final Massieu response wrappers, not in summability or differentiation. An initial repair at Verify2 `33a7cd593c48a4ae8ed2dacb569d778f0c8ef99b` was tested by cold #891. That run gave the terminal diagnosis: after `convert H using 1`, the derivative goal is definitionally closed by `unfold fisherBB` / `unfold fisherBE` / `unfold fisherEE`; every following `ring` is a dead tactic and cold Lean reports `no goals to be solved`.

The proof was reduced to that minimal definitional closure and pushed as Verify2:

`a60fb703389bef3a0cbe00587406a7c446e77bd3` — `Remove dead Hessian normalization tactics`.

Do not call the Hessian cold-certified until the new changed-Lean run is green.

Target covariance entries remain

- `F_BB=(M2 Z-M1^2)/Z^2`,
- `F_BE=(M3 Z-M1 M2)/Z^2`,
- `F_EE=(M4 Z-M2^2)/Z^2`.

## Celestial / Yang–Mills generalized cuts

Scalar raised-box regulator closure remains certified: `J_epsilon(S,T) -> 1/6`.

For the generic nonzero-mu Ds=4 raw sewing define `x=1-beta cos(theta)` and `r=rho^2=1-beta^2`. Exact symbolic reduction gives

`C4_same = 2(r^2-8r+8)/x^2`,

and, using `u=-r+2x-x^2`,

`C4_mixed = 2x^2-8x+(4r+24)-(8r+32)/x+2(r^2+8r+8)/x^2`.

This is an exact separation into polynomial, simple-pole and double-pole pieces. It is topology-reduction input, **not** yet an assignment of box/triangle/bubble master coefficients. That assignment still requires the actual generalized-cut/integrand-reduction map plus conventions.

The exact assertions were added to `discovery/generalized_cuts/generic_ds4_topology_projection_audit.py` at `875f354ff57b4ecd92651a6d5e6ea59176a49f15`. Generic Ds4 Discovery CI #10 passed, including baseline extraction, the new pole audit and generic Ward identities.

Next amplitude step: connect these pole pieces to the existing Badger s23 triangle/bubble subtraction machinery and derive the nonzero-mu topology projector before convention matching. Gravity/higher-loop promotion remains downstream.

## Principal series / completed zeta / Weil

No RH promotion. Delta=2s, critical-line half-density unitarity, completed-zeta response and local Gamma/Wiener-Hopf positivity remain structural. The unresolved global theorem is unconditional positivity of the genuine completed prime-plus-Archimedean Weil quadratic form on an adequate test class, including required closure/interpolation control.

## Spectral / Mehler-Fock / Wiener-Hopf / chamber convolution

No retraction. Active continuous target:

`rho_c(x)=2^(2c-1)/(pi Gamma(2c)) |Gamma(c+i x)|^2`,

`Fourier(rho_c)(t)=sech^(2c)(t/2)`,

`rho_c*rho_d=rho_(c+d)`.

Preferred formal route remains Euler Beta -> logistic substitution -> Fourier normalization -> uniqueness. The continuous heat-subordinator representation remains compatible. Highest-value formal promotion is the arbitrary-c Beta/logistic Fourier theorem; no Barnes axiom or Plancherel over-identification is justified.
