# Codex/GPT all-fronts continuation — 2026-09-04 16:20 EDT

Scope: Codex/GPT work only. Claude-owned work was not inspected.

## Verify2 / prime-gas fluctuation geometry

Cold changed-Lean #890 failed at Verify2 `d8f0b09fcbcd384df47c8c8cbb4b8389e22e4b51` in the final Massieu response wrappers only. The upstream countable moment derivatives remain certified. The failure was tactic-shape residue from `rw [neg_div]; congr 1 <;> ring`, not a mathematical failure.

Repaired `NumberGibbsQuadraticMassieuHessian.lean` by unfolding the common-denominator Fisher entries directly and closing the resulting quotient numerator identity with `ring`. New Verify2 head:

`33a7cd593c48a4ae8ed2dacb569d778f0c8ef99b` — `Normalize Hessian response numerators directly`.

At record time changed-Lean #891 and full Build #2037 are still running. Do not call the Massieu Hessian cold-certified until #891 is green.

Target entries remain

- `F_BB = (M2 Z - M1^2)/Z^2`,
- `F_BE = (M3 Z - M1 M2)/Z^2`,
- `F_EE = (M4 Z - M2^2)/Z^2`,

with the already-certified moment derivatives giving the four thermodynamic response identities once the wrapper source passes cold CI.

## Celestial / Yang–Mills generalized-cut front

The scalar raised-box regulator endpoint remains certified: `J_epsilon(S,T) -> 1/6`.

The raw generic nonzero-mu Ds=4 sewing is angle-dependent, so normalization alone cannot turn it into a topology coefficient. This run sharpened that obstruction by using the physical propagator coordinate

`x = 1 - beta cos(theta)`, `r = rho^2 = 1-beta^2`.

The same-helicity sewing becomes exactly

`C4_same = 2 (r^2 - 8 r + 8) / x^2`.

For mixed helicity, since `u = beta^2(1-cos^2 theta) = -r + 2x - x^2`, the exact Laurent decomposition is

`C4_mixed = 2 x^2 - 8 x + (4 r + 24) - (8 r + 32)/x + 2(r^2 + 8r + 8)/x^2`.

This separates polynomial, simple-pole and double-pole content algebraically. It is **not yet** a box/triangle/bubble coefficient assignment: that requires the actual generalized-cut/integrand-reduction map and convention normalization. The point is to expose the precise pole data that the topology extractor must consume.

Implemented exact SymPy assertions in `discovery/generalized_cuts/generic_ds4_topology_projection_audit.py` and pushed

`875f354ff57b4ecd92651a6d5e6ea59176a49f15` — `Extract exact propagator-pole decomposition of generic Ds4 sewing`.

Discovery generic Ds4 CI #10 passed, including baseline extraction, topology audit and generic Ward audit.

Next amplitude step: connect these pole pieces to the existing Badger s23 triangle/bubble subtraction machinery and derive the actual nonzero-mu topology projector before convention matching. Gravity/higher-loop promotion remains downstream.

## Principal series / completed zeta / Weil

No RH promotion this run. The positive-real half-density/principal-series dictionary, Delta=2s, critical-line unitarity, completed-zeta response and local Gamma/Wiener-Hopf positivity remain structural results. The unresolved global theorem remains unconditional positivity of the completed prime-plus-Archimedean Weil quadratic form on an adequate test class (with the needed closure/interpolation control).

## Spectral / Mehler-Fock / Wiener-Hopf / chamber convolution

No retraction. The continuous target remains

`rho_c(x) = 2^(2c-1)/(pi Gamma(2c)) |Gamma(c+i x)|^2`,

`Fourier(rho_c)(t) = sech^(2c)(t/2)`,

`rho_c * rho_d = rho_(c+d)`.

The current exact discovery route is Euler Beta -> logistic substitution -> Fourier normalization/uniqueness. The continuous heat-subordinator representation recorded previously remains compatible with this route. Highest-value formal promotion remains the arbitrary-c Beta/logistic Fourier theorem; no Barnes axiom or Plancherel over-identification is justified.
