# Codex/GPT research run — 2026-09-01 18:19 America/Toronto

## Scope and separation

Worked only on the Codex/GPT track under `website/CODEX.md`. Claude's separate work, branches, records, and `CLAUDE.md` were not inspected.

## Verify2 state and number thermodynamics

Starting `GPPVerify2:codex/lean-workbench` head: `296226a6be1c2a4a4a4419caab4f45cacdf24efa` (`Add quadratic number-Gibbs thermodynamics`). Its changed-Lean run `33560791611` was still marked `in_progress` at inspection time.

Pushed new Verify2 commit `53cdafc0185cd1881e2322ea9abeecb6ee95a5e8` adding `GppVerify/RiemannHypothesis/NumberGibbsQuadraticFisherGeometry.lean`.

New exact bridge:

\[
\det \operatorname{Cov}(X,X^2)
= \frac{N_F(m_0,m_1,m_2,m_3,m_4)}{m_0^4}
\]

for nonzero mass `m0`, where `N_F` is the already-formalized division-free Fisher numerator.

Specialized target for the quadratically confined number-Gibbs family:

\[
w_n(\beta,\eta)=\exp[-\beta L_n-\eta L_n^2],\qquad L_n=\log(n+1),
\]

and for every real `beta` with `eta > 0`, the normalized covariance determinant of the sufficient statistics `L` and `L^2` is strictly positive. The proof uses the certified strict mass-aware numerator plus `Z>0`, so no differentiability/Hessian assumption is smuggled in.

Fresh changed-Lean run `33565823512` was `pending` immediately after the push. Do not call the new endpoint certified until it compiles.

## Thermodynamic interpretation

The previous thermodynamics module defines

\[
Z=\sum_n w_n,\quad U=M_1/Z,\quad V=M_2/Z,
\]

and the exact two-coupling Gibbs entropy

\[
S=\log Z+\beta U+\eta V.
\]

The new Fisher endpoint is the correct nondegeneracy statement for the two sufficient statistics before proving parameter derivatives of `log Z`. The next analytic/formal layer should identify the Hessian of `log Z` with this covariance matrix by justified termwise differentiation; after that, strict covariance determinant positivity becomes strict Hessian positive-definiteness.

## Scalar box / amplitudes

Re-audited `RaisedBoxConcreteVolumeClosure.lean`: the physical affine-simplex normalization is already exact,

\[
\operatorname{Vol}(\Delta_3)=1/6,\qquad J_0(S,T)=1/6.
\]

No new Beta/Gamma estimate is required. The remaining scalar regulator theorem is still the actual measure-theory composition: nested boundary faces are null; strict-interior pointwise convergence is promoted to AE convergence; AE strong measurability is discharged; then inner/middle/outer interval dominated convergence yields `J_epsilon -> 1/6`.

Do not promote Yang-Mills/gravity numerator claims before this scalar regulator limit closes. After closure, proceed to honest fixed-loop-momentum `D_s=4`, `mu != 0` state sums, allowing boxes/triangles/bubbles/rational sectors to be determined by sewing rather than assumed.

## Principal series / completed zeta / Weil

Focused source mining recovered the manuscript claim `Delta = 2s` and its exact shadow-functional-equation compatibility `Delta -> 2-Delta` iff `s -> 1-s`. Treat the algebraic dictionary and already-formalized critical-line response as local/principal-series infrastructure only. The RH boundary remains global: independently construct the prime-plus-Archimedean quadratic form, identify it exactly with the Weil explicit-formula criterion on an adequate test class, and prove positivity without encoding unknown zeros.

## Spectral / Mehler-Fock / Wiener-Hopf / chamber

No theorem-sized new result was needed this run. Existing exact Gamma/Mehler-Fock recurrence, real-part positivity, Wiener-Hopf factorization and chamber-polynomial results remain salvageable mathematics. Do not infer global Weil positivity from local spectral positivity.

## Source mining

The uploaded focused loops paper still presents the intended celestial cut -> Mellin -> regulated scalar box -> fixed-channel dispersion chain. Its `complete derivation` wording remains stronger than the current formal state because the regulator/DCT passage is not yet closed. Treat it as source material, not as a certified endpoint.

## Persistence note

A direct read/write attempt to Supabase `codex.research_notes` / `codex.formalization_queue` was blocked by the connector safety layer in this run, so this Discovery2 note is the durable fallback record. Retry Codex-schema Supabase persistence on the next run; do not write to Claude/public schemas.

## Exact next frontiers

1. Read/repair changed-Lean run `33565823512` for `53cdafc...`; if green, normalize the two-parameter Fisher geometry at changed-module level.
2. Formalize justified derivatives of `Z` / `log Z` in `(beta, eta)` and identify the `2 x 2` Hessian with `Cov(L,L^2)`.
3. Attack the first actual AE/nested-DCT scalar-box theorem, not another majorant surrogate.
4. Continue global Weil-form identification work while preserving the local/global separation.
5. Continue exact spectral/chamber salvage and inspect one plausible scaffold/axiom when CI latency permits.
