# Codex/GPT research rotation — 2026-09-06 16:21 EDT

## Scope

Codex/GPT track only. No Claude branch, context, notes, or CLAUDE.md inspected. Re-anchored from website/CODEX.md, Supabase codex records, GPPVerify2:codex/lean-workbench, GPPDiscovery2:codex/discovery-workbench, and the GPP file archive.

## Verify2 CI repair

GPPVerify2 head fbe4b57faaaa46ffe1f670db967566514a51f8d3 had full Build #2085 green but cold changed-Lean #939 red in `GppVerify/QuantumGravity/SpectralRhoContinuousStep.lean`.

The strict pairwise theorem assumes

`hxy : x ^ 2 ≠ y ^ 2`

and needs strict positivity of

`(y ^ 2 - x ^ 2) ^ 2`.

Cold Lean did not infer the nonzero difference through `positivity`. The repair is explicit:

- `hdiff : y ^ 2 - x ^ 2 ≠ 0 := sub_ne_zero.mpr (Ne.symm hxy)`;
- derive strict square positivity using `mul_self_pos` and `pow_two`;
- retain the existing positive coefficient proof.

Repair commit: `ba1d6e083830e83c8f0543e6f4e96b22f8346cb3`.

Fresh cold #940 and Build #2086 are running. No theorem statement or mathematical claim was weakened.

The target exact identity remains

\[
(F_c(y)-F_c(x))(y^2-x^2)
=\frac{2}{c(2c+1)}(y^2-x^2)^2,
\qquad c>0,
\]

with global nonnegativity and strict positivity when `x^2 ≠ y^2`.

## Source mining: principal-series spectral moments

Library source `Principal_Series_Kinematic_Blocks(1).tex` contains an independently proved exact moment package for

\[
P(\lambda)=\frac{\pi\lambda}{\sinh(\pi\lambda)},
\qquad
D(\lambda)=\Re\psi\!\left(\tfrac12+\tfrac{i\lambda}{2}\right)-\psi\!\left(\tfrac12\right).
\]

The source gives

\[
A_0=\frac14,
\qquad
A_1=\frac18,
\qquad
A_2=\frac18,
\]

where

\[
A_k=\frac1{2\pi}\int_{\mathbb R}P(\lambda)D(\lambda)^k\,d\lambda.
\]

Therefore under the normalized probability measure

\[
\frac{4P(\lambda)}{2\pi}\,d\lambda,
\]

one has the exact independent fluctuation statement

\[
\mathbb E[D]=\frac12,
\qquad
\operatorname{Var}(D)=\frac14.
\]

This is a clean salvage/formalization target for the principal-series/Mehler-Fock spectral front. It does not imply RH positivity and does not identify a loop master coefficient.

## Other active fronts

- Scalar celestial cut -> dispersion -> raised-box regulator closure remains certified at `J_epsilon(S,T) -> 1/6`.
- Yang-Mills blocker remains the factor-preserving opposite crossed full-conic tree with uncut denominators retained through topology subtraction before forming `C^(4)=C^(V_m)-C^(S)` and applying Badger projection. No master coefficient promoted.
- Prime-gas countable two-parameter curvature endpoint remains `R(beta,eta)<1/2` for real beta and eta>0.
- RH/Weil boundary unchanged: finite zero-paired PSD equivalence is formal, but unconditional completed prime-plus-Archimedean Weil positivity is still missing.

## Next boundary

1. Terminal #940/#2086 and repair immediately if needed.
2. If green, formalize the normalized spectral-moment consequence `E[D]=1/2`, `Var(D)=1/4` only after locating/reusing the existing exact A0/A1/A2 infrastructure or proving the analytic integral layer honestly.
3. Continue arbitrary-c chamber semigroup via heat-mixture convolution before Gamma-density identification.
4. Continue YM with the explicit opposite crossed full-conic tree rather than collapsed sewing surrogates.
