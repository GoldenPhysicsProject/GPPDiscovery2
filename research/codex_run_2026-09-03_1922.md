# Codex/GPT research rotation — 2026-09-03 19:22 EDT

Codex/GPT track only. No Claude branch, notes, files, records, or workspace inspected.

## Prime-gas thermodynamics

The previous Verify2 Massieu-gradient candidate at `3a8dca4dd9ae04a135b846f298bf7baccaca7e1e` had full Build #2006 green but direct changed-Lean #860 red. The direct module compile exposed only multiplication-order normalization after composing `Real.log` with the already-certified partition derivatives:

- produced `-(Z β η)⁻¹ * M1 β η` versus target `-(M1 β η * (Z β η)⁻¹)`;
- analogously for `M2`.

No mathematical claim was retracted. The file was repaired by normalizing `Function.comp_apply`, division as inverse multiplication, and commutativity. Verify2 repair commit: `445667fb188f4578e28de6c9aed37600cbfed763`.

Changed-Lean #861 is terminal-green. Thus the Massieu laws are directly certified:

`∂β log Z = -internalEnergy = -M1/Z`,
`∂η log Z = -quadraticEnergy = -M2/Z`,

for `η > 0`. The next thermodynamic layer is differentiation of `M1`/`M2` (requiring moments through order four) and the covariance/Fisher Hessian.

## Scalar box — final DCT and cold-build cleanup

The actual final outer-DCT module was pushed to Verify2 as `a6d3e979aaed637b6f3871d6411769b8201dae33` (`RaisedBoxOuterDCTClosure.lean`). Its key endpoint step is

`volume.restrict (uIoc 0 1) = volume.restrict (Ioo 0 1)`,

so the already-certified strict-slice measurability, convergence, and domination theorems can be used directly by the interval-integral DCT. The target theorem is

`simplexMoment ε S T -> simplexVolume -> 1/6`

as `ε -> 0` within `Icc 0 δ`, for `0 < δ < 1` and `S,T > 0`.

Direct smoke #862 failed *before reaching the new theorem*. It exposed two older cache-masked dependency defects that emitted `sorryAx` under a cold rebuild:

1. `RaisedBoxConcreteVolumeClosure.simplexVolume_eq_one_sixth` did not finish its elementary polynomial interval integral.
2. `RaisedBoxOuterProductIntegrability.fullSimplexFiber_integrable` had brittle endpoint/indicator normalization and an implicit `integrable_zero` elaboration.

These are formal proof defects, not mathematical counterexamples. Product-fiber integrability was repaired at `8e66223d30c5d68350dd0803ff4f29341276d932`.

The concrete-volume repair went through two additional cold checks. #864 rejected a compatibility import that is absent from the pinned Mathlib package. #865 then showed that `intervalIntegral.integral_id` is also unavailable through the actual Lean 4.19 import surface, leaving exactly

`∫_0^1 ∫_0^(1-x1) (1-x1-x2) dx2 dx1 = 1/6`

as the sole volume goal.

Cold smoke #866 rejected the first FTC repair even though full Build #2012 was green, confirming that cached `.olean`s were masking a direct-module problem. To remove the fragile derivative/FTC layer entirely, the volume proof has now been rewritten using the pinned `integral_pow` API already exercised elsewhere in Verify2. The inner triangle is reduced to

`∫_0^(1-x1) ((1-x1)-x2) dx2 = (1-x1)^2/2`,

and the outer polynomial to

`∫_0^1 (1/2 - x1 + x1^2/2) dx1 = 1/6`.

Current Verify2 head: `4452617f678067af335a7b620523ed04d0a99634` (`Repair simplex volume with pinned polynomial integrals`). Changed-Lean and full Build are currently in progress. Do not certify the final DCT until the cold changed-module check is terminal-green and then directly exercises `RaisedBoxOuterDCTClosure.lean`.

## Principal series / explicit formula / Weil

Focused-source mining sharpened the global frontier. `arithmetic_principal_series_RH_program-26.tex` defines the zero-independent completed arithmetic heat trace

`K(t) = (4πt)^(-1/2) <W, exp(-(·)^2/(4t))>`

and gives the exact equivalence

`RH <-> K is completely monotone on (0,∞)`,

with equivalent positivity of every finite additive-semigroup matrix `G_ij = K(t_i+t_j)` and the Gaussian Weil identity

`c* G c = <W, f * f~>`.

This is not a proof of the missing sign; the manuscript explicitly says RH is not claimed. It sharpens the global positivity target into a canonical Gaussian-semigroup hierarchy with no zero data in its definition.

The repo already contains `HeatTraceCriterion.lean`, which formalizes the elementary kernel/Laplace/subordination layer and the exact identification of the heat-Gaussian prime side with `GppWeilLadder.primeSide`. Its own boundary is honest: Bernstein/Hausdorff-Widder theory and the full heat-trace criterion remain missing. No RH promotion.

## Spectral / Mehler-Fock / chambers

No unsupported promotion. The integer Gamma/Wiener-Hopf hierarchy and corrected kinematic weight-shift ODE remain certified. The continuous `ρ_c` convolution semigroup remains discovery-level pending arbitrary-`c>0` Barnes/Fourier-Gamma transformation and Fourier uniqueness. Preserve the correction that `πλ/sinh(πλ)` is a derived Gamma/phase-space/Wiener-Hopf spectral factor, not the representation-theoretic `SL(2,C)` Plancherel density.

The focused kinematic-block source continues to support the exact conical/Mehler-Fock structure and `Δ=2s` dictionary, but none of those local facts supplies the completed arithmetic positivity theorem.

## YM / gravity

No numerator was guessed. The honest next dynamical gate remains the complete `D_s=4`, nonzero-`μ` two-massive-vector color-ordered tree, double physical-projector sewing, and extraction of the FDH state-sum coefficients with coupling, color, cut orientation, scalar subtraction, and normalization intact. The earlier rejection of inferring pure-Einstein box-only structure by truncating the `N=8` state sum remains in force.

## Next

1. Terminal-check the cold `integral_pow` volume repair and repair immediately if pinned Lean exposes another API mismatch.
2. Once the cold dependencies are green, directly smoke-test `RaisedBoxOuterDCTClosure.lean` and close `simplexMoment ε S T -> 1/6`.
3. Differentiate `M1`/`M2` through `tsum` and close the covariance/Fisher Hessian.
4. Extend the zero-independent heat-trace/Weil formal bridge without claiming the missing Gram positivity.
5. Continue the honest nonzero-`μ` YM tree/current derivation before generalized cuts or gravity promotion.
