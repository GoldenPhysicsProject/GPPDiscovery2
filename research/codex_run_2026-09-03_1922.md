# Codex/GPT research rotation — 2026-09-03 19:22 EDT

Codex/GPT track only. No Claude branch, notes, files, records, or workspace inspected.

## Prime-gas thermodynamics / CI repair

The previous Verify2 Massieu-gradient candidate at `3a8dca4dd9ae04a135b846f298bf7baccaca7e1e` had full Build #2006 green but direct changed-Lean #860 red. The direct module compile exposed only multiplication-order normalization after composing `Real.log` with the already-certified partition derivatives:

- produced `-(Z β η)⁻¹ * M1 β η` versus target `-(M1 β η * (Z β η)⁻¹)`;
- analogously for `M2`.

No mathematical claim was retracted. The file was repaired by normalizing `Function.comp_apply`, division as inverse multiplication, and commutativity. Verify2 repair commit: `445667fb188f4578e28de6c9aed37600cbfed763` (`Repair Massieu derivative normalization`).

On that exact head, changed-Lean #861 and Build #2007 were in progress at the end of the rotation. Do not call the Massieu laws certified until changed-Lean is terminal-green.

Target laws remain

`∂β log Z = -internalEnergy = -M1/Z`,
`∂η log Z = -quadraticEnergy = -M2/Z`,

for `η > 0`.

## Scalar box

No new analytic obstruction. The certified stack still reduces closure to the final outer `x1` dominated-convergence assembly. Inputs already present in Verify2 include the physical nested-coordinate bridge, middle DCT, a.e. nested measurability on the strict physical interval, the norm bound by `1 + (S*x1)^(-δ)/(1-δ)`, integrability of that outer majorant for `0 < δ < 1`, and the zero-regulator normalization `simplexMoment 0 S T = simplexVolume = 1/6`.

The remaining bookkeeping is the null endpoint transfer at `x1=1` plus the final interval-integral DCT invocation. Target remains `simplexMoment ε S T -> 1/6`.

## Weil / principal series

No RH promotion. Preserve `Δ=2s` and critical-line/principal-series local structure, but the global missing theorem remains positivity of the actual completed prime-plus-Archimedean Weil quadratic form on one concrete admissible transform class together with finite pair-support interpolation for that same class. Local Gamma/Wiener-Hopf/chamber positivity is not a substitute.

## Spectral / Mehler-Fock / chambers

No unsupported promotion. The integer Gamma/Wiener-Hopf hierarchy and corrected kinematic weight-shift ODE remain certified. The continuous `ρ_c` convolution semigroup remains discovery-level pending arbitrary-`c>0` Barnes/Fourier-Gamma transformation and Fourier uniqueness. Preserve the correction that `πλ/sinh(πλ)` is a derived Gamma/phase-space/Wiener-Hopf spectral factor, not the representation-theoretic `SL(2,C)` Plancherel density.

## YM / gravity

No numerator was guessed. The honest next dynamical gate remains the complete `D_s=4`, nonzero-`μ` two-massive-vector color-ordered tree, double physical-projector sewing, and extraction of the FDH state-sum coefficients with coupling, color, cut orientation, scalar subtraction, and normalization intact. The earlier rejection of inferring pure-Einstein box-only structure by truncating the `N=8` state sum remains in force.

## Next

1. Terminal-check changed-Lean #861 / Build #2007; repair immediately if direct smoke still finds a pinned-Mathlib normalization issue.
2. Once Massieu gradients are green, attack countable second derivatives / covariance Hessian.
3. Push the final scalar outer DCT to `simplexMoment ε S T -> 1/6`.
4. Continue honest YM tree/current derivation and the global Weil positivity bridge without status inflation.
