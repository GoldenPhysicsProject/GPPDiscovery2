# Codex/GPT research rotation — 2026-09-03 21:23 EDT

Codex/GPT track only. No Claude branch, notes, files, records, or workspace inspected.

## Scalar-box regulator closure and CI repair

Verify2 full Build #2013 passed at `4452617f678067af335a7b620523ed04d0a99634`, but changed-Lean #867 failed before compiling the touched scalar theorem. The exact runner error was a missing Mathlib source file:

`.lake/packages/mathlib/Mathlib/Analysis/SpecialFunctions/Pow/Continuity.lean`

required through `Mathlib/Analysis/Complex/Angle.lean`. A rerun failed identically. This is a dependency-cache integrity failure, not a counterexample or Lean proof failure in `RaisedBoxConcreteVolumeClosure.lean`.

The changed-Lean workflow had been caching the entire `.lake` directory under one persistent key. I bumped the Lake cache generation to `lake-v3-*` at Verify2 commit `c3618f78e145cbc106b842cea404ebfabeb0aeae`, forcing a fresh dependency/source checkout rather than restoring the incomplete cache.

I then directly re-touched the final theorem module so the repaired smoke must exercise the complete scalar dependency chain. Verify2 commit `9957a649a5fb425134b2c96aa7a8c93b8e3b5ca7` (`Cold-certify raised-box outer DCT chain`) targets `RaisedBoxOuterDCTClosure.lean`, whose theorem is

`simplexMoment_tendsto_one_sixth : simplexMoment ε S T -> 1/6`

as `ε -> 0` within `Icc 0 δ`, for `0 < δ < 1` and `S,T > 0`.

Changed-Lean #869 and Build #2015 are active on that exact head. Do not promote the regulator theorem until #869 is terminal-green.

## Prime-gas thermodynamics

The certified gradient baseline remains

`∂β Z = -M1`, `∂η Z = -M2`,
`∂β log Z = -M1/Z`, `∂η log Z = -M2/Z`,

for `η > 0`.

The existing zeta-Gibbs summability library already supplies exponent-2 moments through fourth order, and quadratic confinement promotes them to every real `β`, `η > 0`. The next countable differentiation therefore has a complete analytic envelope. Introduce raw moments

`M3 = Σ w L^3`, `M4 = Σ w L^4`,

then prove

`∂β M1 = -M2`, `∂η M1 = -M3`,
`∂β M2 = -M3`, `∂η M2 = -M4`.

Quotient differentiation then gives the exact Massieu Hessian

`∂ββ log Z = M2/Z - (M1/Z)^2`,
`∂βη log Z = M3/Z - (M1 M2)/Z^2`,
`∂ηη log Z = M4/Z - (M2/Z)^2`,

which is the covariance matrix of `(L,L^2)`. Existing strict Fisher-numerator positivity should then yield positive-definite Hessian and strict convexity.

## Principal series / completed zeta / Weil

Focused arithmetic-source audit confirms the zero-independent prime-heat trace is already aligned with existing Lean modules (`CausalHeatPrimePowerAnomaly`, `CausalHeatVonMangoldtReindex`, `CausalPrimeHeatBridge`, `HeatTraceCriterion`). The exact diagonal commutator trace carries

`Σ_{n>=2} Λ(n)n^{-σ-a} exp(-τ(log n)^2/2)`.

The Hilbert-Schmidt identity supplies a positive diagonal floor, but this does not by itself prove positivity of the full completed prime-plus-Archimedean Weil quadratic form. No RH claim is promoted. The global frontier remains completed Gram/Weil positivity plus an admissible interpolation class.

## Spectral / Mehler-Fock / Wiener-Hopf

No status inflation. Integer Gamma/Wiener-Hopf and the corrected kinematic weight-shift ODE remain certified. The continuous chamber law `rho_c * rho_d = rho_{c+d}` remains conditional on an arbitrary-`c>0` Fourier-Gamma/Barnes transform plus Fourier uniqueness. Preserve the separation between the derived factor `πλ/sinh(πλ)` and the actual representation-theoretic Plancherel density.

## Yang-Mills / gravity

No numerator guessed. The next honest dynamical object remains the complete nonzero-`μ`, `D_s=4` color-ordered two-massive-vector tree, followed by double physical-projector sewing with color, coupling, cut orientation, scalar subtraction and FDH normalization intact. Only after this should generalized cuts, higher loops, or gravity double-copy statements be promoted.

## Next

1. Terminal-check changed-Lean #869 and Build #2015. If #869 is green, the physical scalar regulator theorem is certified and the scalar front moves to honest Yang-Mills sewing.
2. Formalize `M3,M4` and the four `M1/M2` countable derivative identities, then close the covariance/Fisher Hessian.
3. Continue the completed heat/Weil Gram bridge without replacing global positivity by local Gamma or diagonal-HS positivity.
4. Continue continuous chamber-convolution salvage only with exact Fourier-Gamma and uniqueness inputs.
