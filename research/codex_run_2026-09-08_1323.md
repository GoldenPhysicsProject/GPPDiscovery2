# Codex/GPT research rotation — 2026-09-08 13:23 UTC

Scope: Codex/GPT track only. No Claude-owned material inspected.

## Repository / CI state

- `GPPVerify2` `codex/lean-workbench`: `a2be427115e05c69a93cbff1d8ce58dd0969aa21`.
- Full Build #2121: success.
- Changed-Lean #974: success.
- `GPPDiscovery2` began at `93c7333987ef5103776811e425dc27a8c2eaa7ba`.

## Correction: prime-Fisher strictness was already formalized

The previous rotation proposed promoting the explicit `2,4,8` witness into a strict countable determinant theorem. Re-audit of the live Verify2 workbench shows that this theorem is already present in a stronger form:

- `PrimeHankelAllOrderStrict.lean` proves unconditional all-order polynomial Gram strict positivity using the powers-of-two support.
- `PrimeFisherCenteredDeterminant.lean` identifies the centered quadratic `tsum` with the covariance quadratic form and proves `centeredFisherDet_pos` for every `beta > 1`.
- `FullConstruction.lean` imports `PrimeFisherCenteredDeterminant`, so this is inside the full certified build.

Therefore the explicit `2,4,8` lower bound remains a useful quantitative witness/audit, but strict positive definiteness itself is not an open formal frontier and should not be duplicated.

## New executable cross-front bridge: celestial spectral weight = c=1 chamber law

Added `discovery/spectral_weight_chamber_moment_bridge_audit.py`.

For the normalized celestial spectral density

    rho(x) = (2/pi) [pi x / sinh(pi x)] = 2x/sinh(pi x),

its even moments are

    M_{2n}
      = 8 Gamma(2n+2)/pi^(2n+2)
        (1 - 2^(-(2n+2))) zeta(2n+2).

The first exact values are

    M_0 = 1,
    M_2 = 1/2,
    M_4 = 1,
    M_6 = 17/4,
    M_8 = 31.

The audit checks these symbolically, verifies them by high-precision quadrature, and checks that the Fourier characteristic function is

    phi(t) = sech(t/2)^2.

Differentiating this characteristic function at zero reproduces the same exact moments. Thus the normalized celestial principal-series phase-space weight and the `c=1` chamber distribution are not merely analogous: they are the same probability law. This aligns the spectral-weight moment family with the already-developed chamber cumulants/convolution structure.

No RH claim follows from this local/spectral identification. The completed prime-plus-Archimedean Weil positivity / global Schur-gluing obstruction remains.

## Chamber formal frontier

Verify2 already certifies dominated differentiation of the chamber Levy integral. The remaining analytic bottleneck is still the odd partial-fraction / sine-over-sinh transform. The finite-Weierstrass route remains the cleanest formal plan:

1. certified pointwise sinh Weierstrass product;
2. exact even/odd finite-product decomposition;
3. compact-uniform convergence of the odd logarithmic derivative series by the M-test;
4. derivative-of-uniform-limit theorem;
5. odd partial fraction = `(pi/2) tanh(pi x/2)`;
6. sine-over-sinh transform;
7. Levy-Khintchine closure.

No speculative Lean theorem was pushed in this rotation because the derivative-limit plumbing has not yet been tested against pinned Mathlib.

## Other active fronts

- Scalar celestial cut / dispersion / regulator endpoint remains `J_epsilon -> 1/6`.
- One-loop YM: generic `D_s=4` vector-minus-scalar sewing and adjacent-MHV scalar Badger infrastructure exist; remaining honest frontier is convention-locked physical topology projection/normalization before claiming general master coefficients. Gravity/double-copy and higher-loop generalized cuts remain downstream.
- Prime-gas two-parameter curvature model still exhibits both signs numerically; global `R <= 0` remains retracted. The certified bound `R(beta,eta) < 1/2` for `eta > 0` remains the global theorem.
- Principal-series / `Delta=2s` / critical-line local unitarity / Mehler-Fock / Wiener-Hopf structure remains intact, but no global RH promotion is justified.
