# Codex/GPT research run — 2026-09-01 19:22 America/Toronto

## Scope and separation

Worked only on the Codex/GPT track: `GPPVerify2:codex/lean-workbench`, `GPPDiscovery2:codex/discovery-workbench`, Codex/GPT records, focused sources, and CODEX operating boundaries. Claude work was not inspected.

## CI and number thermodynamics

Verify2 commit `53cdafc0185cd1881e2322ea9abeecb6ee95a5e8` (`Add normalized quadratic number-Gibbs Fisher geometry`) is now changed-Lean green: GitHub Actions run `33565823512` completed successfully.

Thus the exact normalized bridge is certified at changed-module level:

\[
\det \operatorname{Cov}(L,L^2)
=\frac{N_F(m_0,m_1,m_2,m_3,m_4)}{m_0^4}>0
\]

for the quadratically confined number-Gibbs family with every real `beta` and `eta > 0`.

No Hessian claim is promoted yet: parameter differentiation of `Z` / `log Z` still has to be justified before identifying this covariance matrix with the thermodynamic Hessian.

## Scalar-box advance

Pushed Verify2 commit `4d14f1f7f16ffb86666be5d3aff782e96d3e82b0`, adding `GppVerify/CelestialHolography/RaisedBoxInnerAE.lean`.

The new theorem isolates the first actual measure-theory step needed for regulator closure. For fixed strict-interior `x1,x2`,

\[
0<x_1,\quad 0<x_2,\quad x_1+x_2<1,
\]

the raised-box integrand satisfies

\[
Q(S,T;x_1,x_2,x_3)^{-\varepsilon}\to 1
\]

for Lebesgue-almost every `x3` in `Ioc 0 (1-x1-x2)`. The only missing strict-interior point in that half-open interval is the upper endpoint `x3 = 1-x1-x2`, and `volume.ae_ne` removes that singleton.

This is stronger operationally than the previous global-boundary plan: because the proof is nested interval DCT, the boundary can be removed one interval endpoint at a time. No global three-dimensional simplex-null-set theorem is required for the first DCT stage.

Fresh changed-Lean run `33570885757` is in progress for `4d14f1f7...`; do not call the new AE theorem certified until that run completes.

## Other fronts

No new Yang-Mills/gravity numerator was promoted. Honest fixed-loop `Ds=4`, `mu != 0` state sewing remains downstream of scalar regulator closure.

No new global Weil/RH theorem was promoted. The `Delta=2s`, principal-series, completed-zeta response, and exact Archimedean/Wiener-Hopf infrastructure remain local structure only; the missing global step is still the prime-plus-Archimedean Weil quadratic-form identification and positivity.

No new Mehler-Fock/chamber theorem was needed this run. Existing exact chamber recurrence and positivity results remain valid but logically separate from global Weil positivity.

## Exact next frontier

1. Read and repair run `33570885757` if the new inner-AE theorem fails.
2. Once green, package the first actual inner interval-DCT theorem using the existing one-channel majorant and interval-integrability certificate.
3. Then lift the resulting inner integral to the middle and outer DCT stages, ending at `simplexMoment epsilon S T -> 1/6`.
4. In parallel, begin justified termwise differentiation of the quadratically confined partition sum to connect the now-certified covariance determinant to the Hessian of `log Z`.
