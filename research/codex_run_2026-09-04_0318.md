# Codex/GPT all-fronts continuation — 2026-09-04 03:18Z

## Objective

Continue the Codex/GPT Golden Physics track across the scalar-regulator, dimensional-unitarity, positive-real/Weil, prime-gas thermodynamics, and spectral/chamber fronts. No Claude-owned work was inspected.

## Architecture

### Scalar box / regulator

Verify2 head entering this rotation is `63ccc362eb3547865b805711952d8a2bca5989be`, repairing the pinned-Mathlib import for the concrete raised-box volume closure. Full Build #2016 is green. Changed-Lean #870 has passed checkout, source-sorry gate, toolchain/cache setup, and Mathlib cache retrieval, and has reached the actual changed-module compilation step. Therefore the previous missing-Mathlib-source/cache failure has not recurred. The final outer-DCT theorem remains present on the branch with target

`simplexMoment ε S T -> 1/6`

for `0 < δ < 1`, `S,T>0`, and `ε -> 0` through `0 <= ε <= δ`. Do not promote this theorem until #870 is terminal-green.

### Prime-gas thermodynamics

The countable first derivatives are already formalized:

- `∂β Z = -M1`,
- `∂η Z = -M2`,
- `∂β log Z = -M1/Z`,
- `∂η log Z = -M2/Z`.

A fresh source audit shows that the exact termwise second-gradient numerators are already present:

- β derivative of `w(-L)` is `w L^2`,
- η derivative of `w(-L)` is `w L^3`,
- η derivative of `w(-L^2)` is `w L^4`.

Quadratic confinement already supplies summability through the fourth logarithmic moment. Hence no new analytic majorant is needed for the Hessian layer. The next countable identities are

`∂β M1=-M2`, `∂η M1=-M3`, `∂β M2=-M3`, `∂η M2=-M4`,

which imply

`∂ββ log Z = M2/Z - M1^2/Z^2`,
`∂βη log Z = M3/Z - M1 M2/Z^2`,
`∂ηη log Z = M4/Z - M2^2/Z^2`.

This is exactly the covariance/Fisher matrix of `(L,L^2)`. Existing strict infinite Fisher-numerator positivity then supplies the determinant route to positive definiteness and strict convexity.

### Honest Yang–Mills / gravity cuts

The executable `double_massive_vector_projector_audit.py` confirms the correct kinematic sewing discipline for the exposed rank-two 5D current: the raw tensor is not separately Ward-transverse against an arbitrary unphysical state, while the double 4D massive-projector contraction agrees with the explicit `3×3=9` physical polarization state sum. This remains a kinematic/state-sum audit, not yet the complete physical FDH numerator derivation.

The dynamical frontier is therefore unchanged: derive the full `D_s=4`, nonzero-μ color-ordered two-massive-vector tree, perform the double physical-projector sewing, then do scalar subtraction with coupling/color/cut orientation/normalization intact before promoting any generalized-cut, higher-loop, or gravity statement.

### Positive reals / principal series / Weil

The exact half-density representation remains clean:

`(U_a f)(x)=a^(1/2)f(ax)`, so on `f_s=x^(-s)` the eigenvalue is `a^(1/2-s)`, whose modulus is one exactly on `Re(s)=1/2`. The unitary inversion `Jf(x)=x^(-1)f(1/x)` sends `s -> 1-s`. Under `Delta=2s`, these become `Re(Delta)=1` and `Delta -> 2-Delta`.

This is representation theory only. It does not constrain zeta zeros. The missing global theorem remains positivity of the actual completed prime-plus-Archimedean Weil quadratic form on one concrete admissible transform class together with the needed finite interpolation / multiplier closure for that same class.

### Spectral / Mehler–Fock / Wiener–Hopf / chambers

The exact integer convolution-power family remains valid:

`rho_m(x)=2^(2m-1)/(π Γ(2m)) |Γ(m+ix)|^2`,
`hat rho_m(k)=sech^(2m)(k/2)`,
`rho_m=rho^{*m}`,

and

`rho_m(x)=2^(2m-2)/Γ(2m) * prod_{j=1}^{m-1}(j^2+x^2) * rho(x)`.

Thus finite repeated gluing stays inside a positive polynomial-times-base-density family. The continuous `c>0` law remains discovery-level until arbitrary-c Fourier-Gamma/Barnes transformation plus Fourier uniqueness is formalized. Preserve the terminology correction: `πλ/sinh(πλ)` is a derived Gamma/Wiener-Hopf spectral factor, not the representation-theoretic `SL(2,C)` Plancherel density.

## Interfaces

- Verify2: `codex/lean-workbench` only.
- Discovery2: `codex/discovery-workbench` only.
- Codex Supabase reads were connector-blocked this rotation; no database state was inferred or fabricated.
- No Claude-owned branch, records, notes, or files inspected.

## Verification

- Verify2 Build #2016: green on `63ccc362...`.
- Verify2 changed-Lean #870: still running at the changed-module compile step at record time.
- Scalar final theorem: candidate/pending cold certification, not promoted yet.
- Prime-gas Hessian formulas: exact mathematical consequence with all required local derivative and summability inputs already formalized; countable second-derivative theorem not yet pushed.
- YM/gravity: double-projector equality is executable discovery evidence; full physical numerator still missing.
- RH: no promotion; completed Weil positivity remains missing.

## Next frontier

1. Terminal-check #870. If green, promote the raised-box regulator theorem to certified and move the scalar line downstream into the physical Yang–Mills sewing.
2. Push the countable `M1/M2` derivative layer and then the exact covariance/Fisher Hessian in Verify2.
3. Keep the global arithmetic target on completed Weil positivity rather than local Gamma/principal-series positivity.
4. Formalize the integer Gamma/Mehler–Fock convolution family further before attempting the arbitrary continuous chamber parameter.