# Codex/GPT research rotation — 2026-09-08 11:21 EDT

Codex/GPT track only. No Claude material inspected.

## CI and formal state

- `GPPVerify2` branch `codex/lean-workbench` exact head: `1207fa9ff9e6f61243d05ea9df78131200276166` (`Codex: import normalized spectral-weight moment family`).
- Full Build #2124: **PASS**.
- Changed-Lean #976: **PASS**.
- Therefore `SpectralWeightMomentFamily.lean` and its import into `FullConstruction.lean` are cold-certified on the live workbench.

## Principal-series / chamber advance: Barnes-score bridge

A new executable audit was added at
`discovery/principal_series_barnes_score_bridge_audit.py`.

Starting from

\[
P(\lambda)=\frac{\pi\lambda}{\sinh(\pi\lambda)},\qquad
D(\lambda)=\Re\psi\!\left(\frac12+\frac{i\lambda}{2}\right)-\psi\!\left(\frac12\right),
\]

set \(\lambda=2t\). Duplication gives

\[
P(2t)=\frac1\pi\left|\Gamma\!\left(\frac12+it\right)\right|^2
\left|\Gamma(1+it)\right|^2.
\]

Introduce the two-parameter deformation

\[
K(a,c)=\frac1{2\pi}\int_{\mathbb R}
\frac{\Gamma(a+it)\Gamma(c-it)}{\Gamma(a)\Gamma(c)}
\Gamma(1+it)\Gamma(1-it)\,dt.
\]

Barnes' first lemma gives exactly

\[
\boxed{K(a,c)=\frac{ac}{(a+c)(a+c+1)}}.
\]

At \(a=c=1/2\), \(K=1/8\). Along the symmetric deformation \(a=c=r\), the logarithmic score is

\[
S(t)=2\left[\Re\psi\!\left(\frac12+it\right)-\psi\!\left(\frac12\right)\right]=2D(2t).
\]

The normalized base density is

\[
\boxed{p(t)=\frac{8t}{\sinh(2\pi t)}}.
\]

The rational Barnes normalization gives

\[
\frac{K'}K\Big|_{r=1/2}=1=\mathbb E[S].
\]

Trigamma reflection yields the pointwise parameter derivative

\[
S_r(t)=-\pi^2\tanh^2(\pi t),
\]

and direct elementary integration gives

\[
\boxed{\mathbb E[S_r]=-4}.
\]

Since

\[
\frac{K''}K\Big|_{r=1/2}=-2=\mathbb E[S^2+S_r],
\]

we obtain

\[
\boxed{\mathbb E[S^2]=2}.
\]

Using \(A_k=\frac14\mathbb E[D^k]\) and \(D=S/2\), this recovers

\[
\boxed{A_1=A_2=\frac18}.
\]

This is an independent analytic framework for the higher principal-series moments and is the preferred route toward proving the newly discovered

\[
A_4=\frac38-\log2+2\log^2 2-\frac{\pi^2}{24}.
\]

The new audit is explicit that it does **not** prove the \(A_4\) formula yet.

## Celestial cut / amplitudes

No new master coefficient was promoted this rotation. The scalar celestial cut -> dispersion -> regulator endpoint remains \(J_\varepsilon\to1/6\). The one-loop adjacent-MHV bubble frontier remains the convention-locked physical double-cut boundary plus the required triple-cut subtraction moments; the repository's phase-normal target remains a literature rewrite until that direct cut derivation is complete. Gravity/double-copy and higher-loop generalized cuts remain downstream.

## Positive-real / completed-zeta / Weil

No RH promotion. The local principal-series, half-density, critical-line unitarity, spectral-weight, Mehler-Fock, chamber and Wiener-Hopf results remain intact. The missing arithmetic theorem remains completed prime-plus-Archimedean Weil positivity / global Schur gluing. The Barnes-score bridge is local spectral analysis and is not substituted for that global step.

## Prime-gas thermodynamics

No new curvature theorem this rotation. Strict prime-Fisher positive definiteness is already formalized. For the separate two-parameter zeta-Gibbs model, sampled curvature still has both signs; the global conjecture `R <= 0` remains retracted. The certified global upper bound remains `R < 1/2` on the stated domain.

## Next frontier

1. Use the Barnes deformation plus reflection identities to derive a closed recurrence for higher score moments and attack \(A_3\), then \(A_4\), analytically.
2. Continue the compact-uniform Weierstrass derivative route to the exact sine-over-sinh / `sech^2` Fourier closure in Lean.
3. On amplitudes, do not rebuild threshold state sums; feed the generic physical cut into the Badger/Forde topology projection with all subtraction moments and convention factors retained.
