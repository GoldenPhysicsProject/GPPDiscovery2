# Codex research run — 2026-08-27 13:21 EDT

## Repository/CI reconciliation

- `GPPVerify2` `codex/lean-workbench` began this run at `160e8d6584effebe78949466018ea4a1fb8fb30d` (`Codex: pin complex Gr24 orthogonal scalar`).
- That head was healthy: Build #982 passed, Gibbs differential thermodynamics #159 passed, and sech endpoints #85 passed.
- `GPPDiscovery2` `codex/discovery-workbench` began at `c46f1dcf8bf298983ea9817b2ecd2a7cced52f08`.
- `website/CODEX.md` was re-read and the no-`sorry`, no-axiom-as-proof, discovery-first discipline was followed.
- The Supabase connector was blocked by the tool safety boundary even for a trivial read, so no database record was fabricated; this repository note is the run record.
- Claude's separate work was not inspected.

## New Grassmannian/twistor result

The existing `GooglyAntiunitaryExchange.lean` had already formalized the finite-dimensional antiunitary googly operation on `Λ²ℂ⁴`: complex conjugation plus orientation reversal, with `G² = 1`, norm-density preservation, and `★G = -G★`, hence exchange of the self-dual and anti-self-dual Hodge sectors.

This run adds the missing Plücker-polynomial compatibility.  In the ordered coordinate basis

`(p01,p02,p03,p12,p13,p23)`

define

`Q(v) = v0*v5 - v1*v4 + v2*v3`.

For the existing googly exchange `G`, the exact identity is

`Q(Gv) = -conj(Q(v))`.

Therefore the Plücker quadric `Q=0` is invariant under the antiunitary orientation-reversing conjugation.  Packaging the actual two-frame Plücker coordinates into a six-vector and using the already-proved unconditional Plücker relation gives

`Q(Plücker(v1,v2)) = 0`

and hence

`Q(G(Plücker(v1,v2))) = 0`.

This is the first exact algebraic bridge showing that the operation responsible for the googly/helicity exchange preserves the decomposable-bivector locus associated with `Gr(2,4)`.  It is stronger than merely observing that the same operation swaps the ± Hodge eigenspaces.

### Honesty boundary

This does **not** yet construct the induced anti-linear involution on the actual projective `Gr(2,4)` object, nor prove the full Penrose/Klein transport to celestial operators.  The next Grassmannian target is to projectivize/descend this Plücker action and match it to an actual plane-level conjugation map.

## Verify commits

- `a03314940097d33587044c33d8b05ce30a67e793` — `Codex: preserve Plucker quadric under googly exchange`
- `c24e3363b965728f085364654db9c35c39841ddb` — `Codex: gate googly Plucker geometry in CI`

The Grassmannian workflow now explicitly builds `GrassmannianGr24`, `GrassmannianPluckerProjective`, and `GooglyAntiunitaryExchange`, because the latter was not imported by the root module and could otherwise evade the root build.

At the final check on head `c24e3363b965728f085364654db9c35c39841ddb`, sech endpoint CI #89 had already passed; Build #986 and Gibbs #163 were still running/pending.  The new dedicated Grassmannian push gate had been triggered but had not yet produced a terminal verdict at the reporting cutoff, so the new Plücker theorem is pushed but not yet claimed CI-certified.

## Other active-front state

### Sech / spectral weight

The nonzero-parameter exact whole-line self-convolution is already formalized:

`∫_ℝ dx / (cosh(πx) cosh(π(λ-x))) = 2λ/sinh(πλ)` for `λ ≠ 0`.

The removable `λ=0` normalization `∫ sech²(πx) dx = 2/π` remains a clean exact target.

### Prime-gas thermodynamics

Do not duplicate already-closed work: the genuine Gibbs layer already includes `A'=-U`, `U'=-Var`, strict positive Fisher variance, positive heat capacity, entropy monotonicity, and the cumulant relation `κ₂'=-κ₃` for `β>1`.

### Scalar box / YM / gravity

The scalar regulated physical limit and fixed four-dimensional stripped-MHV external-numerator transfer are formalized.  No promotion to gravity is justified yet: the KLT/gravity cut retains four uncut denominators and still needs a genuine generalized-cut/reduction theorem.  D-dimensional state-sum/rational-term issues remain separate.

### Half-density / RH / Weil

`Δ=2s`, critical-line/unitarity, half-density shadow reflection, and completed-zeta local response are already formalized.  No RH promotion was made.  The missing theorem is still the global transport from the completed explicit-formula/Wiener–Hopf object to the actual Weil paired-form positivity criterion.  Local Poisson/Gamma/cut positivity is not a substitute.

## Next frontier

1. First read the Grassmannian CI verdict and repair the exact new theorem if Lean rejects any conjugation simplification.
2. Descend/projectivize the antiunitary Plücker action to a genuine plane/projective `Gr(2,4)` map and relate it to the plane-conjugation structure.
3. In parallel, close the `λ=0` sech normalization when a clean Mathlib primitive route is available.
4. Continue gravity generalized-cut reduction and global Weil transport without conflating local algebraic positivity with the missing global theorems.
