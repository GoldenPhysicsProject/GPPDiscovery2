# Codex/GPT continuation — 2026-08-31 15:21 EDT

No Claude material inspected or used.

## Verify2 CI repair

The live `codex/lean-workbench` head `c26ded51fde0800a568d958e7985b025bb7bc194` had a deterministic failure in the Grassmannian workflow at `GooglyTwistorLift`, while all preceding Gr(2,4), projective Plucker, quotient-shadow, and antiunitary googly steps were green.

The fragile proofs tried to normalize `Complex.I ^ 2` through simp/ring. The repair pushed to Verify2 commit `6919681b9fef0d57abfbc41034f24f7e134752ee` instead proves the involution and exterior-square identity componentwise with `Complex.ext`, reducing complex multiplication to real/imaginary polynomial identities. This removes dependence on the pinned simplifier recognizing the algebraic relation for `I` in the required normal form. CI was relaunched by the push; do not promote to `main` until the Grassmannian lane and full suite are green.

## Spectral / Mehler–Fock / Wiener–Hopf tail theorem

The already-certified all-order chamber density has the real-axis closed form

\[
\rho_k(x)=\frac{2^{2k+1}}{(2k+1)!}\,
\frac{x}{\sinh(\pi x)}\prod_{j=1}^{k}(j^2+x^2),
\]

with the removable origin filled separately by the existing exact value.

For fixed chamber index `k`, as `|x| -> infinity`,

\[
\frac{x}{\sinh(\pi x)}
\sim 2|x|e^{-\pi|x|},
\qquad
\prod_{j=1}^{k}(j^2+x^2)\sim |x|^{2k}.
\]

Therefore

\[
\boxed{
\rho_k(x)\sim
\frac{2^{2k+2}}{(2k+1)!}
|x|^{2k+1}e^{-\pi|x|}
}
\qquad (|x|\to\infty).
\]

Consequences: every polynomial spectral moment of every fixed chamber is absolutely finite, and the chamber recurrence changes only the polynomial prefactor, never the universal Archimedean exponential scale `e^{-pi |x|}`. This is useful for the Wiener–Hopf/explicit-formula bridge because polynomial observables remain in the same exponentially integrable spectral class at every finite chamber.

This does **not** prove a repeated-sech convolution law and does not alter the global Weil/RH boundary.

## Prime-gas thermodynamics

The previous two-parameter Fisher result remains the active new thermodynamic frontier:

\[
\det g/L_{123}=1+C_\beta e^{-\eta\Delta}+o(e^{-\eta\Delta}),
\qquad
\Delta=(\log4)^2-(\log3)^2,
\]

where the first correction is the unique `(1,2,4)` Vandermonde triple. The rigorous tail estimate over all later triples remains to be formalized. Existing one-parameter entropy/free-energy differential and strict concavity theorems are already formalized; no stronger thermodynamic claim is made in this run.

## Scalar box / amplitudes

The scalar analytic problem remains reduced to the formal measure layer. Pointwise interior regulator removal, the concrete one-channel majorant, endpoint integrability, inner/middle evaluation, outer norm domination, and `J_0=1/6` are already present. The missing theorem is the actual nested `Integrable` / almost-everywhere boundary removal / Tonelli-DCT assembly proving

\[
J_\varepsilon(S,T)\to \frac16.
\]

No additional Beta/Gamma identity is missing. Honest Yang–Mills/gravity cut numerators stay downstream of this closure; ordinary 4D cut matching is not being used to claim recovery of the rational sector.

## Principal series / zeta / Weil boundary

The positive-real half-density, `Delta=2s`, critical-line unitarity, completed-zeta Archimedean response, Mehler–Fock/Gamma weight, and Wiener–Hopf structure remain exact local/Archimedean results. The non-circular global prime-plus-Archimedean identification with the genuine Weil quadratic form and unconditional positivity remains open. No RH claim is promoted.
