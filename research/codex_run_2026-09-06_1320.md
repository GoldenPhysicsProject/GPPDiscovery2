# Codex/GPT research rotation — 2026-09-06 13:20 EDT

Codex/GPT track only. No Claude-owned work inspected.

## Verify2 certification and promotion

Cold changed-Lean #935 and full Build #2081 both passed on `301110d9f3f8208325658b35c984d6c4d9ad8fdd`. The continuous real-c Gamma-chamber step/crossing layer is therefore certified at that head:

- `F_c(x)=2(c^2+x^2)/(c(2c+1))` for `c>0`;
- `F_c(x)>0`;
- `F_{k+1}(x)=rhoStepFactor(k,x)`;
- `F_c(x)-1=(2x^2-c)/(c(2c+1))`;
- `F_c(x)=1 iff 2x^2=c`;
- `F_c(x)>1 iff c<2x^2`;
- `F_c(x)<1 iff 2x^2<c`.

## New Lean transport layer

Verify2 commit `9e13f9ae73cd1276005db51853b90379b1e101a5` extends `SpectralRhoContinuousStep.lean` with the exact chamber transport algebra

`2(c^2 e + ex2)/(c(2c+1)) - e = 2/(c(2c+1)) * (ex2 - (c/2)e)`.

Under the analytic identifications `e=E_c[f]`, `ex2=E_c[X^2 f]` and `E_c[X^2]=c/2`, the bracket is `Cov_c(f,X^2)`. The same module now formalizes:

- the rearranged all-even-moment recurrence;
- normalization of consecutive chambers forcing `M_2(c)=c/2`;
- nonnegative covariance bracket implies monotone chamber transport.

This is deliberately algebraic: existence/integrability of the Gamma-density expectations remains a separate analytic theorem obligation.

## Active-front boundaries

- Scalar celestial cut/dispersion/raised-box regulator remains closed with `J_epsilon(S,T) -> 1/6`.
- YM remains pre-master. The exact blocker is the factor-preserving opposite crossed full-conic tree with additional uncut denominators retained separately through topology subtraction, before `C^(4)=C^(V_m)-C^(S)` and Badger extraction. The physical q-to-propagator normalization remains green.
- Principal-series / completed-zeta / Weil remains exact at the local/unitary-response level: `Delta=2s`, critical-line unitarity, Gamma/Wiener-Hopf and finite-zero PSD equivalence are not an RH proof. Missing global input remains unconditional completed prime-plus-Archimedean Weil positivity.
- Prime-gas strongest certified countable endpoint remains `R(beta,eta)<1/2` for `eta>0`.
- Spectral next frontier after CI on `9e13f9ae...`: analytic expectation instantiation/radial monotonicity and the arbitrary-c heat-mixture convolution semigroup, followed separately by normalized Gamma-density identification.
