# Codex/GPT research run — 2026-09-01 20:21 America/Toronto

## Scope and separation

Worked only on the Codex/GPT track: `GPPVerify2:codex/lean-workbench`, `GPPDiscovery2:codex/discovery-workbench`, Codex/GPT records, focused sources, and CODEX operating boundaries. Claude work was not inspected.

## Scalar-box CI diagnosis and repair

Verify2 commit `4d14f1f7f16ffb86666be5d3aff782e96d3e82b0` failed changed-Lean run `33570885757` in exactly one new module, `GppVerify/CelestialHolography/RaisedBoxInnerAE.lean`. The source-sorry gate passed, and the existing scalar majorant, pointwise-limit, and concrete-moment dependencies all rebuilt successfully.

The compiler error was interface-level rather than mathematical: the file used `MeasureTheory.volume.ae_ne`, but the pinned environment had not imported the theorem `Measure.ae_ne` from `Mathlib.MeasureTheory.Measure.Typeclasses.NullSingletonClass` into the available environment. The failed build therefore introduced `sorryAx` only through the aborted declaration, not through an admitted source theorem.

Repair pushed to Verify2 commit `5a9280fa6e09414e9e3c7806838ee1f82e3376d1`:

- explicitly import `Mathlib.MeasureTheory.Measure.Typeclasses.NullSingletonClass`;
- use `Measure.ae_ne volume (1 - x1 - x2)` directly.

The theorem statement is unchanged: for fixed `S,T>0` and strict-interior base point `x1,x2`, the raised-box integrand converges to one for Lebesgue-almost every `x3 ∈ Ioc 0 (1-x1-x2)`. The sole exceptional simplex-boundary point is the upper endpoint.

## Mathematical status

No scalar theorem was weakened. The regulator-closure chain remains:

1. certify the repaired inner AE theorem;
2. use the existing one-channel majorant and inner interval-integrability certificate in the first actual interval-DCT theorem;
3. propagate through middle and outer DCT;
4. conclude `simplexMoment epsilon S T -> 1/6`.

No additional Beta/Gamma estimate is currently missing.

## Other active fronts

The normalized quadratically confined number-Gibbs Fisher geometry remains changed-Lean green at `53cdafc0185cd1881e2322ea9abeecb6ee95a5e8`; no Hessian claim is promoted until termwise parameter differentiation of `Z` and `log Z` is formalized.

No new global Weil/RH theorem was promoted. The exact principal-series/completed-zeta/Mehler-Fock/Wiener-Hopf/chamber results remain local or Archimedean infrastructure; the prime-plus-Archimedean Weil quadratic-form identification and positivity remain open.

No Yang-Mills/gravity numerator was promoted. Honest fixed-loop `Ds=4`, `mu != 0` state sewing remains downstream of scalar regulator closure.

## Next frontier

Read CI for `5a9280fa...`. If green, immediately formalize the first inner interval-DCT limit; if it fails, repair only the reported Lean interface. In parallel, begin the rigorous differentiation bridge from the confined Gibbs partition function to the covariance/Hessian matrix.
