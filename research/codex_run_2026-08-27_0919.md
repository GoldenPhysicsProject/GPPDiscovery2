# Codex/GPT research checkpoint — 2026-08-27 09:19 EDT

Codex/GPT track only. No Claude material inspected.

## CI and formal status

Current `GPPVerify2` `codex/lean-workbench` head before this run was `58a97f8b1d4f23c276b81d245b396da69765ef39`.

- Full Build #913: **green**.
- Sech convolution endpoint gate #16: **green**.
- Gibbs differential thermodynamics #90: red only in `hasDerivAt_zetaLogPartition`, where Lean had reduced the zeta derivative to the exact Gibbs first-moment tsum but had not unfolded the project abbreviation `M1`. No mathematical mismatch was present.
- Repair pushed to `GPPVerify2`: `6c04d596161a672d3431a01d625ea058262f4f77`, making the `M1` normalization explicit.

Therefore the following are now certified by the full build at `58a97f8...`:

1. the Mehler–Fock/Gamma base weight closes continuously at the origin,
   `rhoGamma(0,0) = 2/pi`, matching `2 x / sinh(pi x)` as `x -> 0`;
2. both endpoint limits for the shifted log-cosh primitive,
   `D_lam(x) -> +pi lam` at `+infinity` and `D_lam(x) -> -pi lam` at `-infinity`;
3. the strict zeta Gibbs Fisher bridge on `beta > 1`: the actual log-energy variance equals the positive von-Mangoldt/Fisher tsum and is strictly antitone.

## Sech self-convolution frontier

The derivative theorem already formalized is

`D_lam'(x) = pi sinh(pi lam) / (cosh(pi x) cosh(pi(lam-x)))`.

Together with the certified endpoint jump `2 pi lam`, the exact target for `lam != 0` is

`integral_R dx / (cosh(pi x) cosh(pi(lam-x))) = 2 lam / sinh(pi lam)`.

The `lam = 0` value must remain separate:

`integral_R sech(pi x)^2 dx = 2/pi`.

No zero-denominator substitution into the nonzero-lambda formula is permitted. The remaining Lean work is FTC on finite intervals plus the improper-limit passage. Mathlib's pinned FTC interface `intervalIntegral.integral_eq_sub_of_hasDerivAt` is available and matches the existing global derivative theorem exactly once interval integrability of the explicit smooth derivative kernel is supplied.

## Prime-gas thermodynamics

The genuine response identities on `beta > 1` are now organized as

`Z(beta) = sum n^(-beta)`,
`U(beta) = M1(beta)/Z(beta)`,
`g(beta) = Var_beta(log n)`,
`A(beta)=log Z(beta)`.

The formal derivative targets are

`A'(beta) = -U(beta)`,
`U'(beta) = -g(beta)`.

The second is already reduced through the complex negative-log-derivative calculus to the certified zeta variance bridge. The first failed only at the abbreviation normalization described above; repair `6c04d596...` is awaiting CI.

## Scalar box / Yang–Mills / gravity honesty boundary

The scalar-box structured regulator limit and the external four-dimensional numerator transfer remain valid. For the MHV Yang–Mills cut, multiplying the scalar remainder by the external numerator is legitimate because the numerator is loop-independent on the cut in the stated four-dimensional convention.

The gravity KLT cut is **not** reduced to the same two-denominator scalar box: its honest cut has four uncut denominators. No scalar-box regulator theorem has been promoted to gravity without an additional denominator reduction or generalized-cut argument.

Next scattering targets remain: prove a four-denominator dominated regulator bound or derive a legitimate partial-fraction/generalized-cut reduction; then formalize higher-loop cut sewing only after the one-loop denominator structure is closed.

## RH / principal-series frontier

The structural dictionary remains certified: positive-real Haar half-density/principal-series unitarity, `Delta = 2s`, shadow/functional-equation reflection, and critical-line fixed locus. The completed-zeta response has the expected shadow oddness on the unitary line away from zeros/poles.

No RH claim follows from the local positive kernels currently formalized. The missing theorem is still a global transport from the completed Weil explicit formula / Wiener–Hopf representation to the finite paired-form positivity criterion. The Gamma–Plancherel defect is fully positive as an integral Gram kernel, but the arbitrary-real digamma identification remains outside the pinned Mathlib library; only the even-natural shift lattice has the exact digamma identification in Lean.

## Next executable frontier

1. clear CI for `6c04d596...` and finish the Gibbs Legendre/entropy derivative layer;
2. formalize finite-interval sech convolution by FTC, then pass both endpoints to obtain the whole-line identity, including the separate `lam=0` theorem;
3. use the exact convolution as the next spectral/chamber kernel input;
4. on scattering, keep Yang–Mills numerator transfer separate from the still-open gravity four-denominator reduction;
5. on RH, attack explicit-formula transport rather than deriving more local positivity corollaries.