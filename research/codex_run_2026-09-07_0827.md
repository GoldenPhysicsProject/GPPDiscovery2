# Codex/GPT research rotation — 2026-09-07 08:27 EDT

## CI re-anchor

- `GPPVerify2:codex/lean-workbench` entered this run at `01e6cd3f826159053ef5367c17f8ebd7d818a527`.
- Full Build #2101 completed successfully on that head. The corresponding cold changed-Lean smoke #955 remains a recurrent slow/hung runner and has not reported a theorem failure.
- `GPPDiscovery2:codex/discovery-workbench` entered at `f6be3cb5abd5d63283db96d92e32c6480c1aff19`.

## New exact chamber/Lévy formal target

For

\[
\nu_c(x)=\frac{c}{|x|\sinh(\pi |x|)},\qquad c\ge 0,
\]

the previously used near-origin estimate is actually global:

\[
|x|^2\nu_c(x)\le \frac{c}{\pi}\qquad (x\in\mathbb R).
\]

At `x=0` this is immediate under Lean's totalized division. For `x\ne0`, use

\[
\pi |x|\le \sinh(\pi |x|)
\]

from `sinh y >= y` for `y>=0`, positivity of the denominator, and cross multiplication. This strengthens the local half of the Lévy weighted-integrability proof and is reusable for compensated-kernel domination.

The theorem was pushed to `GppVerify/CelestialHolography/ContinuousSechLevyKernel.lean` at Verify2 head

`953a07ff97e818f6e375e463ac3df2286384e3f1`

as

`GppContinuousSechLevyKernel.sq_abs_mul_levyDensity_le`.

Fresh Build #2102 and cold smoke #956 are running on that exact head. Do not call this theorem certified until a terminal green build is observed.

## Active-front rotation / boundaries

### Celestial amplitudes → YM/gravity

The scalar celestial-cut/dispersion/raised-box regulator endpoint remains formally closed at `J_epsilon(S,T) -> 1/6`. The physical YM obstruction remains the genuine opposite/pre-sewing full-conic tree: factorwise uncut denominators and opposite-side numerator data must be retained before `C^(4)=C^(V_m)-C^(S)` and Badger `T1/T2/T3` extraction. Existing full-conic covariance, complementary-helicity phase cancellation, and Frobenius sewing invariance do not determine that opposite tree. No YM master coefficient, gravity numerator, or higher-loop coefficient is promoted in this run.

### Principal series / completed zeta / Weil

The half-density / `Delta=2s` / critical-line unitarity / Gamma-Wiener-Hopf / Mehler-Fock layers remain exact local structure. The RH-critical blocker remains unconditional positivity of the genuine completed prime-plus-Archimedean Weil quadratic form on the required test class. The two-grid/Hausdorff reduction is an equivalence/reduction only; no RH promotion.

### Prime-gas thermodynamics

The certified countable quadratic number-gas endpoint remains the genuine strict curvature theorem `R(beta,eta)<1/2` for `eta>0`, with positive-definite Massieu/Fisher geometry and the centered cubic-square semantic bridge already closed. No stronger curvature or thermodynamic theorem was earned this run.

### Spectral / chamber / convolution

The correct next analytic theorem after the global quadratic majorant is still the compensated cosine-integral identity

\[
2c\log\cosh(t/2)=\int_{\mathbb R}(1-\cos(tx))\nu_c(x)\,dx,
\]

or equivalently the negative-sign Lévy-Khintchine exponent for `Phi_c(t)=sech(t/2)^(2c)`. The weighted Lévy condition is analytically proved; the exact integral identification and characteristic-function theorem remain open. The independent Beta/logistic/Fourier route to the explicit Gamma chamber density remains separate and should not be conflated with the transform-side semigroup algebra.

No Claude-owned material was inspected.
