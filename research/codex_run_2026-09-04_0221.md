# Codex/GPT research rotation — 2026-09-04 02:21 EDT

Codex/GPT track only. No Claude-owned branch, notes, files, records, or workspace were inspected.

## Live Verify2 / scalar regulator

`GPPVerify2:codex/lean-workbench` began at `313bee2a9a39c1b95ef45df9348a560174b51230`. Build #2017 was green, but cold changed-Lean #871 terminated red after reaching actual source compilation. The preceding import-path diagnosis was therefore not the whole failure.

Inspection of pinned Mathlib v4.19.0 (`c44e0c8e...`) exposed a concrete source defect in `RaisedBoxConcreteVolumeClosure.lean`: `intervalIntegral.integral_sub` and `intervalIntegral.integral_add` are not argument-free rewrite lemmas. Their signatures require explicit `IntervalIntegrable` witnesses. The file invoked both without those witnesses.

The repair is Verify2 commit `61d1448fc87618e83171e1e8deb41c8f3c551c4b`: constants use `intervalIntegrable_const`, the identity uses `intervalIntegrable_id`, and `x^2/2` uses `(intervalIntegrable_pow 2).div_const 2`. The mathematical normalization remains

`simplexVolume = 1/6`,

feeding the already-written outer dominated-convergence target

`simplexMoment ε S T -> 1/6`

for positive Euclidean invariants and `ε -> 0+` inside `0 <= ε <= δ < 1`. Changed-Lean #872 and Build #2018 were triggered and were still in progress when this record was written. No certification is claimed before the cold smoke is terminal green.

## Prime-gas thermodynamics

The certified first-derivative baseline remains

`∂β Z = -M1`, `∂η Z = -M2`,

hence

`∂β log Z = -M1/Z`, `∂η log Z = -M2/Z`.

The existing pointwise derivative and quadratic-confinement infrastructure reaches logarithmic moments through order four. The next exact countable layer is therefore

`∂β M1 = -M2`, `∂η M1 = -M3`, `∂β M2 = -M3`, `∂η M2 = -M4`.

Consequently the Massieu Hessian is

`Hββ = M2/Z - (M1/Z)^2`,
`Hβη = M3/Z - M1 M2/Z^2`,
`Hηη = M4/Z - (M2/Z)^2`,

which is the covariance matrix of the sufficient statistics `L` and `L^2`. Existing strict Fisher/covariance determinant positivity can then be connected to strict convexity rather than reproved from scratch.

## Continuous Gamma / chamber convolution

The previous discovery-level arbitrary-`c>0` identity survives audit. Euler beta plus the logistic substitution `u=e^y/(1+e^y)` gives

`|Γ(c+ix)|^2 = Γ(2c) 4^{-c} ∫_R sech^{2c}(y/2) e^{ixy} dy`.

With

`ρ_c(x)=2^{2c-1}/(π Γ(2c)) |Γ(c+ix)|^2`,

Fourier inversion yields exactly

`hat ρ_c(t)=sech^{2c}(t/2)`

and therefore, once Fourier uniqueness/convolution is formalized,

`ρ_c * ρ_d = ρ_{c+d}` for `c,d>0`.

This removes the need for a Barnes-contour derivation at the discovery level. Formalization still requires a usable Beta/Gamma integral theorem, the logistic change of variables, and Fourier uniqueness in pinned Mathlib.

## Focused-paper correction: spectral factor versus Plancherel density

The uploaded focused sources `blackbody_law_qg_v1.tex` and `kinematic_block_v1-1.tex` repeatedly call

`P(λ)=π λ/sinh(π λ)=|Γ(1+iλ)|^2`

a `SL(2,C)`/celestial Plancherel weight or measure. This terminology must not be propagated as a representation-theoretic theorem. What is exact and salvageable is the Gamma/Wiener-Hopf/phase-space spectral factor and its critical-line identity under `Δ=2s`, `2s=1+iλ`. The representation-theoretic Plancherel density is a distinct object and requires a separate theorem/normalization. This is a correction of interpretation, not of the exact Gamma identity.

Likewise the Mellin identity for `(2 sinh t)^{-1}` producing `(1-2^{-s}) Γ(s) ζ(s)` is a transform identity and zero reformulation only. It does not prove RH. The global missing step remains positivity of the completed prime-plus-Archimedean Weil form on one concrete admissible class together with the needed interpolation/multiplier closure.

## Yang-Mills / gravity

The existing executable 5D four-gluon audit already uses the complete color-ordered cubic-plus-quartic tree and proves the correct Ward reconstruction. The rank-two audit proves that an exposed two-vector tensor is not separately Ward-transverse against an arbitrary unphysical companion state, whereas the double massive projector equals the explicit 3x3 physical-state sum. Therefore projector bookkeeping is not the remaining dynamical theorem.

The next honest amplitude object is still the physical `D_s=4`, nonzero-`μ` cut numerator obtained from the complete two-massive-vector tree with coupling, color ordering, cut orientation, scalar subtraction, and FDH normalization explicit. No pure-gravity or generalized-cut statement is promoted until that numerator is fixed.

## Next frontier

1. Read terminal #872/#2018. If cold green, directly certify `RaisedBoxOuterDCTClosure.lean` and close the scalar regulator at `1/6`; if red, repair the exact source error, not the cached full-build symptom.
2. Package `M3,M4`, prove the four countable moment derivatives, and identify the Hessian with the already-positive Fisher covariance matrix.
3. Formalize the arbitrary-positive-real Gamma chamber Fourier pair by the Euler-Beta/logistic route.
4. Keep the completed Weil positivity and the physical Yang-Mills numerator as the two global/dynamical hard gates; do not replace either with local positivity or conditional bookkeeping.
