# Codex/GPT all-fronts research rotation — 2026-09-08 19:22 EDT

Codex/GPT track only. Claude-owned work was not inspected.

## CI / formal state

- `GPPVerify2` `codex/lean-workbench` exact head: `bc8bec82f0eb060c78d7bf7ff7c837eaf6d08a65`.
- Full Build #2129: **success** on that exact head.
- Changed-Lean smoke #981: **success** on that exact head.
- Therefore `OddLatticeDerivativeUniform.lean` is now cold-green, not merely a candidate: the compact-uniform M-test for
  `2*x / ((2*n+1)^2+x^2)` is certified and imported into `FullConstruction`.

## Spectral / chamber discovery

Normalize the celestial spectral weight by

\[
\rho(\lambda)=\frac{2}{\pi}\frac{\pi\lambda}{\sinh(\pi\lambda)}
=\frac{2\lambda}{\sinh(\pi\lambda)}.
\]

The exact Fourier transform is

\[
\widehat\rho(t)=\int_{\mathbb R}e^{it\lambda}\rho(\lambda)\,d\lambda
=\operatorname{sech}^2(t/2).
\]

Equivalently,

\[
\log \widehat\rho(t)
=-2\log\cosh(t/2)
=-2\int_0^\infty\frac{1-\cos(tx)}{x\sinh(\pi x)}\,dx.
\]

Thus `rho` is an infinitely divisible symmetric probability law with Levy density

\[
\nu(x)=\frac{1}{|x|\sinh(\pi|x|)}\qquad(x\ne0),
\]

and exact convolution semigroup characteristic functions

\[
\widehat\rho_a(t)=\operatorname{sech}^{2a}(t/2),\qquad a>0.
\]

This packages the desired chamber identity

\[
\int_0^\infty\frac{\sin(tx)}{\sinh(\pi x)}\,dx=\frac12\tanh(t/2)
\]

as the derivative of the Levy exponent. The executable 70-digit audit is
`discovery/spectral/spectral_weight_logistic_levy_audit.py`.

Exactness boundary: the formulas are standard analytic consequences of the sine-over-sinh transform and are independently high-precision audited here. The full Fourier/Lebesgue proof is not yet in Lean; the newly certified odd-lattice uniform convergence module is the formal bridge intended to prove that transform without assuming it.

## Principal series / completed zeta

- Analytic `A4` closure remains intact:
  \[
  A_4=\frac38-\log2+2\log^22-\frac{\pi^2}{24}.
  \]
- No RH promotion. The unresolved RH-bearing step remains completed prime-plus-Archimedean Weil positivity / global Schur gluing. Local principal-series unitarity, Gamma weights, Mehler-Fock, and the new infinitely-divisible chamber law do not imply that arithmetic positivity.

## Celestial amplitudes / generalized cuts

- Scalar cut -> Mellin -> fixed-u dispersion -> raised-box regulator remains closed with `J_epsilon -> 1/6`.
- Generic Ds=4 YM remains blocked at a precise point: the existing full-conic audits prove transverse covariance, complementary-helicity phase cancellation, and the z-independent conversion from q-residue to the physical adjacent-propagator residue, but they explicitly do **not** construct the opposite crossed physical tree. Without that crossing map, vector-minus-scalar physical sewing and factor-preserving Badger T1/T2/T3 topology subtraction cannot honestly produce a master coefficient.
- Gravity remains closed only for the scoped four-graviton all-plus benchmark; generic helicity/multiplicity and higher-loop generalized cuts remain downstream.

## Prime-gas thermodynamics

- Certified strict radial entropy/Fisher identity remains:
  \[
  \beta\partial_\beta S+\eta\partial_\eta S
  =-(\beta,\eta)g(\beta,\eta)^T<0
  \]
  on the confined domain.
- The separate scalar-curvature conjecture `R <= 0` remains retracted; certified global envelope remains `R < 1/2` for `eta > 0`.

## Next frontiers

1. Formal chamber: identify the certified odd-lattice tsum with `(pi/2)*tanh(pi*x/2)` and then derive the sine-over-sinh integral and Levy exponent in Lean.
2. Amplitudes: construct the opposite-tree crossing map on the full conic, then perform the actual Ds=4 vector-minus-scalar sewing before Badger subtraction.
3. Arithmetic: keep local spectral positivity separate from the missing completed Weil/Schur theorem.
4. Principal series: begin honest Barnes-first-lemma/parameter-differentiation formalization rather than premise-only wrappers.
