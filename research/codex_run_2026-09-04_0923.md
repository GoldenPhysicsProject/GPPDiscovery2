# Codex/GPT research rotation — 2026-09-04 09:23 EDT

Codex/GPT track only. No Claude-owned branch, records, files, notes, or workspace were inspected.

## Scalar-box closure

`GPPVerify2:codex/lean-workbench` commit `4554134498ee6133f5b3d4e26ba8e3ac285bc226` passed cold changed-Lean #877 and full Build #2023, including source-sorry, build-log sorry, axiom, and stub/scaffold gates. The raised-box outer dominated-convergence chain is therefore source-certified. The physical endpoint is

`simplexMoment epsilon S T -> 1/6`

for the stated domain `0 < delta < 1`, `0 <= epsilon <= delta`, `S,T > 0` as epsilon tends to zero. The scalar regulator front is closed and the amplitude front advances to honest Yang-Mills normalization/sewing.

`main` was not force-advanced: comparison shows workbench and main have diverged, with five main-only commits after the merge base. Forcing the workbench ref onto main would discard certified main-side corrections. Reconciliation must preserve both histories, and the newly pushed prime-gas head is also still under CI.

## Prime-gas thermodynamic geometry

Added raw moments

- `M3 = sum w L^3`
- `M4 = sum w L^4`

at Verify2 `2b9aa485b8dc4eab239e1d55f579602927273e8a`.

Added candidate `NumberGibbsQuadraticMomentDerivatives.lean` at `6eb9c8b660f88d796a8c98ef3431cbd4e1170e83`, promoting the existing exact summand derivatives through the countable sum using the already-certified all-index quadratic envelope:

- `d_beta M1 = -M2`
- `d_eta M1 = -M3`
- `d_beta M2 = -M3`
- `d_eta M2 = -M4`.

Changed-Lean #879 and Build #2025 are pending. If certified, the next theorem is the exact Massieu Hessian

`H_bb = M2/Z - M1^2/Z^2`,
`H_be = M3/Z - M1*M2/Z^2`,
`H_ee = M4/Z - M2^2/Z^2`,

followed by identification with the already-strict normalized Fisher/covariance matrix.

## Yang-Mills / gravity

Re-audited the Discovery2 generic nonzero-mu engine. It uses exact 5D-null kinematics with `beta^2+rho^2=1`, the ordering `(massive, gluon, gluon, massive)`, and the full stripped color-ordered four-gluon tree (two cubic channels plus quartic contact). The engine intentionally omits overall coupling/color/cut-orientation/loop-measure conventions. Therefore the next honest dynamical boundary is to lock those conventions, implement the FDH scalar subtraction, and identify the resulting physical sewing with the Verify2 `C4` rational baseline. No numerator was guessed or promoted. Generalized cuts, higher loops, and gravity remain downstream.

## Principal series / completed zeta / Weil

The existing certified half-density package already proves `Delta=2s`, shadow `s -> 1-s` maps to `Delta -> 2-Delta`, `Re Delta=1 iff Re s=1/2`, and critical-line dilation unitarity with shadow equal to Hermitian conjugation. This remains local representation-theoretic structure, not an RH proof. The global blocker remains construction and unconditional positivity of the actual completed prime-plus-Archimedean Weil quadratic form on an adequate admissible class, with the required multiplier/interpolation closure.

Drive mining re-located the focused `arithmetic_principal_series_RH_program-26.pdf` corpus for continued global-Weil source work; no zero-location claim was promoted from it this run.

## Spectral / Mehler-Fock / Wiener-Hopf / chambers

The continuous positive-real Gamma chamber target remains

`rho_c(x)=2^(2c-1)/(pi Gamma(2c))*|Gamma(c+i x)|^2`,
`rhoHat_c(t)=sech(t/2)^(2c)`,
`rho_c*rho_d=rho_(c+d)` for `c,d>0`.

Pinned Mathlib already supplies the Beta/Gamma integral infrastructure. The remaining formal boundary is the logistic change of variables and Fourier normalization/uniqueness. No Barnes axiom is justified, and the Gamma/Wiener-Hopf weight remains distinct from a genuine `SL(2,C)` Plancherel density.
