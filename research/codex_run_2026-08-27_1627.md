# Codex run — 2026-08-27 16:27 EDT

## Scope

Codex/GPT track only. No Claude material inspected. Read the live `codex/lean-workbench`, `CODEX.md`, pinned Mathlib v4.19.0, and Codex Discovery2 state.

## Spectral / Wiener–Hopf advance

The raw Wiener–Hopf weight in `MehlerFockSpectralWeight.lean` is

\[
W(\lambda)=\frac{\pi\lambda}{\sinh(\pi\lambda)}.
\]

Lean's totalized division makes the literal raw expression equal to `0` at `lambda=0`, although analytically the singularity is removable with value `1`. The existing extension file already assigned that value and proved positivity/nonvanishing for every real spectral parameter.

This run strengthened the extension by introducing

\[
S_{\rm ext}(x)=
\begin{cases}
1,&x=0,\\
\sinh x/x,&x\ne0,
\end{cases}
\]

and proving the intended identity

\[
W_{\rm ext}(\lambda)=S_{\rm ext}(\pi\lambda)^{-1}.
\]

The target continuity theorem is derived directly from `sinh'(0)=1` using Mathlib's divided-difference theorem `HasDerivAt.continuousAt_div`, followed by continuity of inversion at the nonzero value `1`. This is the mathematically correct removable-singularity argument; no limit is postulated.

Verify2 commits:
- `f1af5500c17f0e78a475704abc929a2dfbdc2b88` — add continuous-extension proof.
- `2e9df4dc0540cac96c6346e7b26aeca2be263b39` — add the extension module to the dedicated sech/Wiener–Hopf CI gate.
- `2e26cf84c7c09b94f0893e5ba2faea5846f5eb33` — repair the import to pinned Mathlib v4.19 (`Trigonometric.Deriv`).

The first dedicated CI attempt failed before elaborating the theorem because `Mathlib.Analysis.SpecialFunctions.Trigonometric.DerivHyp` exists in newer Mathlib but not in pinned v4.19.0. Existing `SechConvolutionClosedForm` passed in the same run. This was an infrastructure/API mismatch, not a mathematical counterexample. The repaired run is pending/running at this checkpoint.

Important correction to stale notes: `RiemannHypothesis/SechSquaredIntegral.lean` proves

\[
\int_0^\infty u\,\operatorname{sech}^2u\,du=\log 2,
\]

not the plain global `sech^2` normalization. Thus the explicit `lambda=0` convolution integral remains a distinct theorem unless one identifies it via the continuous-extension argument plus continuity of the convolution side.

## Celestial cuts / Yang–Mills / gravity audit

The generalized-cut frontier is further advanced than the stale queue suggested.

Already formalized:

1. Universal `mu^(2r)` radial shell family (`Mu2kRadialIntegral.lean`): for `r=k+1`,

\[
\int_0^\infty \tanh x\,\operatorname{sech}^{2r}x\,dx=\frac1{2r},
\]

with exact tail law and normalized survival function.

2. The `mu^8` all-plus gravity shell is the `r=4` member:

\[
\int_0^\infty \tanh r\,\operatorname{sech}^{8}r\,dr=\frac18.
\]

3. `Mu4DimensionShiftAlgebra.lean` proves the exact algebraic cancellation

\[
[-\epsilon(1-\epsilon)]\frac1{6\epsilon}
=-\frac{1-\epsilon}{6}\to-\frac16.
\]

But that file explicitly does **not** prove the analytic Feynman-integral residue

\[
I_4^{(8-2\epsilon)}=\frac1{6\epsilon}+O(1).
\]

Therefore the honest next D-dimensional amplitude target is not another radial-shell normalization. It is the analytic raised-dimension scalar-box pole/residue (or an equivalent dimension-shift theorem), followed by assembly with the actual `mu^4`/`mu^8` cut numerators and generalized-cut reconstruction. Gravity's four-uncut-denominator issue remains separate from the already-proved scalar two-denominator regulator theorem.

## RH / half-density boundary

No RH promotion this run. The removable Wiener–Hopf spectral weight is useful local/spectral infrastructure, but by `CODEX.md` standards it is not the missing RH theorem. The unresolved target remains identification of a completed global explicit-formula/Wiener–Hopf quadratic object with the genuine Weil paired form on an adequate test class, followed by unconditional positivity. Local Plancherel/Poisson/sech positivity is not substituted for that step.

## Prime-gas thermodynamics

No duplicate theorem added. Existing Codex Lean already contains the substantive differential hierarchy (`A'=-U`, `U'=-Var`, strict Fisher positivity, positive heat capacity, entropy monotonicity, and `kappa_2'=-kappa_3`). Current Gibbs CI is being retriggered only because the branch moved.

## Next executable frontier

1. Get the repaired Wiener–Hopf continuity theorem green under pinned Mathlib; repair elaboration only if CI exposes another issue.
2. Use that extension to close the `lambda=0` convolution normalization cleanly if possible, ideally yielding one all-real convolution/weight theorem rather than a split nonzero/zero API.
3. On amplitudes, attack the analytic `I_4^(8-2 epsilon)` pole/residue needed by the `mu^4` dimension shift, then connect to honest D-dimensional Yang–Mills/gravity cuts.
4. Keep the global Weil bridge separate and exact; do not infer RH from local kernel positivity.
