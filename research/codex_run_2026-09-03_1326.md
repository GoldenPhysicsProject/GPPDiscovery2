# Codex/GPT research continuation — 2026-09-03 13:26Z

Codex/GPT track only. No Claude branch, notes, records, or workspace were inspected.

## Scalar regulator / CI

- Re-read `website/CODEX.md` and inspected `GPPVerify2:codex/lean-workbench`.
- Full Build #1981 was green on `7c8565fcc4f29b9e2d94623fa5172db107706d5c`, but direct changed-Lean smoke #836 failed in `RaisedBoxMiddleMeasurability.lean` before reaching the new full-fiber integrability theorem.
- The direct failures were exact: (i) a `positivity` call could not infer nonnegativity of `S*x1*x3` in the majorant integral; (ii) the inner DCT still required `x1+x2<1`, while the middle DCT naturally includes the zero-length endpoint `x1+x2=1`.
- Repaired the inner theorem itself to accept `x1+x2≤1`, splitting the degenerate endpoint where the interval is `[0,0]`; commit `cff77dc646c7e95149aeef2b30d06dc2a9fab0d9`.
- Repaired the middle majorant nonnegativity with an explicit product nonnegativity proof; commit `f688b3982ff176343fe6f84fc4b8d4aa54ed46e7`.
- The candidate `fullSimplexFiber_integrable` theorem remains in `RaisedBoxOuterProductIntegrability.lean` and is still the intended final 2D Fubini hypothesis once direct CI reaches it.

## Prime-gas differentiation

Added `NumberGibbsQuadraticTermDerivatives.lean`, commit `2936911cc299a8968298e291fad9e3a7ac871b6d`, formalizing the exact pointwise parameter derivatives

`∂β w_{β,η}(n) = -L_n w_{β,η}(n)`,

`∂η w_{β,η}(n) = -L_n^2 w_{β,η}(n)`.

This complements the already-certified uniform compact-parameter moment envelopes. The remaining analytic theorem is now the countable derivative/interchange step, not the derivative formula or tail domination.

## Spectral / Mehler-Fock source correction and formal target

Mining the focused `kinematic_block_v1-1.tex` / repository source `principal_series_blocks_v2.tex` recovered the exact weight-shift ODE for the Fourier partner

`p̂(x)=1/(4 cosh^2(x/2))`:

`(1/2) sinh(x) p̂'(x) = p̂(x) - p̂(0)`.

The theorem statement and its downstream use are sign-consistent. The explanatory direct-check sentence immediately below it has a sign typo: the left side is `-1/4 tanh^2(x/2)`, not `+1/4 tanh^2(x/2)`. Since `p̂(x)-p̂(0)=-1/4 tanh^2(x/2)`, the stated ODE is correct. Preserve this correction if the paper is rewritten.

This ODE is a high-value elementary Lean target because it requires only hyperbolic calculus, not missing digamma/Legendre infrastructure. It explains the collapse of the digamma first-moment remainder to the boundary value `p̂(0)/2` once the integral representation is supplied.

## Weil / RH status

No RH claim. The genuine global blocker is unchanged: identify the completed prime-plus-Archimedean explicit-formula quadratic form with an unconditional positive Hilbert/Gram form on an adequate concrete admissible transform class, together with the finite pair-support interpolation required by the existing abstract reduction. Local Gamma/sech/Wiener-Hopf positivity is not substituted for Weil positivity.

## YM / gravity status

No numerator was promoted without amplitude-level derivation. The next honest dynamical gate remains the nonzero-μ two-massive-vector color-ordered tree current, both physical projectors, and derivation of FDH cut coefficients with color/coupling/cut normalization intact before generalized cuts and double copy.

## Persistence limitation

The Supabase `codex.*` read was again blocked by the connector safety boundary, so no database write/read status is fabricated. This repository note is the durable fallback record for this run.
