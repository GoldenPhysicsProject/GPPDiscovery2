# Codex/GPT research continuation — 2026-09-07 11:19 EDT

## Certified state recovered

- `GPPVerify2:codex/lean-workbench` entered this run at `8fb386cd5c2a4b4a00cdf6518793eb820354476b`.
- Full Build #2106 passed on that exact head, including source-sorry gate, full Lake build, build-log sorry gate, axiom audit, and stub census.
- Therefore the inverse-square tail bounds are certified:
  - `nu_c(x) <= c/(pi |x|^2)` for `c >= 0`, `x != 0`;
  - `K_c(t,x) <= 2c/(pi |x|^2)` for the compensated kernel.
- Together with the already-certified global uniform bound
  `K_c(t,x) <= c t^2/(2 pi)`, this gives an elementary bounded-core + inverse-square-tail route to integrability. Exponential tail estimates are true but not logically needed for this step.

## New formal push

A new Verify2 module `ContinuousSechLevyMeasurability.lean` was pushed at
`e0729164ba2d6682a67b2eb173cbd22ac0184aca`.
It targets exact Borel measurability of the totalized density and compensated kernel, and hence `AEStronglyMeasurable` for the latter. Changed-Lean CI #960 was pending at the time of this record. No integral identity is asserted there.

## Chamber / Levy exact analytic route

For

`nu_c(x) = c/(|x| sinh(pi |x|))`, `c >= 0`,

`K_c(t,x) = (1-cos(tx)) nu_c(x)`,

the established pointwise bounds imply the explicit piecewise majorant

- `K_c(t,x) <= c t^2/(2 pi)` on `|x| <= 1`,
- `K_c(t,x) <= 2c/(pi x^2)` on `|x| > 1`.

Hence

`integral_R K_c(t,x) dx <= c(t^2+4)/pi < infinity`.

The decisive remaining identity is

`integral_R K_c(t,x) dx = 2c log cosh(t/2)`.

A useful proof architecture, independent of numerical quadrature, is differentiation under the integral:

1. `I_c(t) := 2c integral_0^infinity (1-cos(tx))/(x sinh(pi x)) dx`.
2. After domination is established, differentiate to
   `I_c'(t) = 2c integral_0^infinity sin(tx)/sinh(pi x) dx`.
3. Use the classical exact transform
   `integral_0^infinity sin(tx)/sinh(pi x) dx = (1/2) tanh(t/2)`.
4. Since `I_c(0)=0`, integrate to obtain
   `I_c(t)=2c log cosh(t/2)`.

For eventual formalization, step 3 can itself be reduced to the positive exponential series

`1/sinh(pi x) = 2 sum_{n>=0} exp(-(2n+1) pi x)`

and the elementary Laplace-sine integral

`integral_0^infinity exp(-a x) sin(tx) dx = t/(a^2+t^2)`,

followed by the standard odd-denominator partial-fraction expansion of `tanh(t/2)`. This makes the remaining special-function input explicit instead of hiding it inside a transform table.

## Celestial scalar / YM / gravity audit

The focused source `loops_from_cuts_celestial_v19.pdf` was re-read. Exact surviving structure:

- cut phase space is the round celestial-sphere measure with antipodal cut legs;
- Mellin image of the two-particle cut is
  `Phi(Delta5,Delta6;M) = (1/(8 pi)) (M/2)^(Delta5+Delta6-2) Gamma(Delta5)Gamma(Delta6)/Gamma(Delta5+Delta6)`;
- `Delta5+Delta6=2` is the scale-invariant shadow locus, not a residue pole;
- on `Delta5=1+i lambda`, `Delta6=1-i lambda`, the factor `pi lambda/sinh(pi lambda)` is a phase-space Gamma factor, not a replacement Feynman/Plancherel loop measure;
- the regulated scalar-box cut has an exact one-parameter integral and closed `artanh` form;
- fixed-channel dispersion becomes Mellin multiplication by the universal `8 pi^2/sin(pi sigma)` kernel in its stated strip.

No honest YM master coefficient was derived this run. The blocking object remains the factor-preserving opposite/pre-sewing full-conic physical tree, with all uncut propagators retained before vector-minus-scalar state sewing and Badger `T_1,T_2,T_3` extraction. Gravity and generalized higher-loop cuts stay downstream.

## Principal series / completed zeta / Weil

No RH promotion. The exact local principal-series/half-density, `Delta=2s`, shadow involution, critical-line conjugation/unitarity, Gamma/Mehler-Fock, and Wiener-Hopf structures survive. The global obstruction remains unconditional positivity of the completed prime-plus-Archimedean Weil quadratic form on an adequate zero-independent test class.

## Prime-gas thermodynamics

No stronger curvature inequality was earned in this run. The certified endpoint remains the countable two-parameter quadratic number-Gibbs geometry with `R(beta,eta) < 1/2` for `eta>0`, together with the existing entropy/free-energy/cumulant/Hessian/Fisher infrastructure. There is still no justification for strengthening this to a sign theorem such as `R <= 0`.

## Scaffold audit

A search of public `main` still finds substantial honest `True := trivial` scaffolds in Yang-Mills/Wightman/RH/GR modules. These are not to be counted as proofs. None was retired in this run because the candidates inspected require missing Wightman/affine-Kac-Moody/adelic or differential-geometric infrastructure rather than a small Mathlib substitution.

## Next boundary

1. Terminalize/repair Verify2 CI for `e0729164...`.
2. Package compact-core and inverse-square tails into `Integrable (fun x => K_c(t,x))`.
3. Formalize the sine-over-sinh transform or an equivalent Euler-product route, then close the Levy-Khintchine exponent.
4. Independently attack the opposite full-conic YM tree and the global completed Weil positivity boundary.

No Claude-owned branch, note, file, or record was inspected.
