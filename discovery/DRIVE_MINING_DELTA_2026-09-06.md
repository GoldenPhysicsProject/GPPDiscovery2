# Drive mining delta — 2026-09-06

## Scope

Chronological mining of the GPP Drive archive focused on googly/shadow/zitterbewegung, mass–time–charge orientation, scaled versus scale-invariant structure, bosonic versus fermionic structure, the U(1)/SU(2)/SU(3) versus literal SU(1) issue, arithmetic wave–particle duality, and formalization consequences for RH, celestial cuts, spectral weights, Grassmannian geometry, prime-gas work, and stub/axiom retirement.

The governing rule remains: later correction layers supersede earlier physical identifications unless the earlier mathematics survives independently. Numerical checks do not upgrade conceptual identifications to theorems.

## 1. Chronology: holographic_chain v10-2 -> v13

Source v10-2 (Drive file `1FkyF-Wi29GvbGXpzaEPHmdqDAZLQUJNf`) still fused a large number of structures into one physical narrative: Riemann reflection, celestial shadow, Grassmannian complement, CPT/time orientation, triality/generations, Bost–Connes, mass, and neutrino claims.

The v13 revision notes (Drive file `1SuCsbdNUVGam3ENLqW1QQfM0fqKU1UMT`) explicitly replace that architecture with a theorem-first spine and demote or remove the failed physical identifications. This supersession is authoritative for promotion into Verify2.

### Surviving exact spine

For a normed division algebra of real dimension d, projective inversion has radial half-density action

`J_d f(r) = r^{-d} f(r^{-1})`

on `L^2(R_+, r^{d-1} dr)`, and Mellin reflection

`M[J_d f](z) = M[f](d-z)`.

The critical/unitary axis is therefore `Re z = d/2`.

Under the unitary conjugation

`C_d f(r) = r^{d/2} f(r)`, 

all these weighted inversions become the same multiplicative Haar inversion on `L^2(dr/r)`:

`C_d J_d C_d^{-1} = J_0`, `J_0 g(r)=g(r^{-1})`.

This is an exact separation between scale covariance and scale invariance: the factor `r^{-d}` is the half-density Jacobian correction, while the Haar representation removes that dimension-dependent weight.

The doubling map `D(z)=2z` exactly intertwines `z -> d-z` with `z -> 2d-z`; for d=1 -> 2 this gives the precise analytic relation behind `Delta = 2s` and maps `Re s=1/2` to `Re Delta=1`. This is a reflection dictionary, not RH and not a proof that physical celestial shadow equals Wigner T.

### Corrected representation-theoretic scope

v13 explicitly distinguishes the radial Mellin realization from the full nonlocal Knapp–Stein/conformal-shadow integral. Therefore Verify2 should not label a radial inversion theorem as a proof of the complete celestial shadow transform.

### Correct Lorentzian real structure

v13 removes the positive-definite Hermitian-complement fixed-locus claim. The surviving Grassmannian real structure uses the signature-(2,2) pseudo-Hermitian complement; its fixed maximal isotropic 2-planes are parametrized by U(2), matching compactified Minkowski space / the Shilov boundary of `SU(2,2)/S(U(2) x U(2))`.

This gives a mathematically meaningful Grassmannian/celestial bridge without identifying `Gr(2,4)` itself with the celestial sphere.

## 2. Googly/shadow/T/zitterbewegung/orientation verdict

No later source repairs the old equality chain

`googly = shadow = T = CPT orientation`.

It remains superseded.

The exact structures that survive independently are:

- celestial parameter reflection `(Delta,J) -> (2-Delta,-J)`;
- Wigner time reversal is a distinct antiunitary operation and does not become identical to the celestial shadow transform merely from parameter reflection;
- worldline orientation versus representation dualization gives the Wilson-line identity `W_{R*}(gamma)=W_R(gamma^{-1})`;
- in the Abelian sign reduction, charge orientation c and worldline orientation t enter through the product `ct`, so simultaneous `(c,t)->(-c,-t)` preserves the oriented gauge datum;
- zitterbewegung retains the exact Dirac frequency `omega_Z = 2mc^2/hbar` but does not by itself establish a cosmological two-sheet ontology;
- the Grassmannian orientation map's Z4 behavior, where separately formalized, is a geometric orientation structure and should not be relabelled as physical T without an equivariant dictionary theorem.

Conceptually, these can be compared through orientation/dualization/involution structure, but they are not one operation.

## 3. New formalization-quality correction: v10's SD/ASD reading of the middle Schubert multiplicity is superseded

v10-2 identified the two middle Schubert classes and the `zeta(s-2)^2` factor with self-dual/anti-self-dual Hodge sectors.

v13 explicitly removes this identification. The surviving geometry is:

- the two ruling families of maximal projective planes on the Klein quadric are the two half-spin components;
- they represent the middle Schubert classes `sigma_2` and `sigma_{1,1}`;
- the coefficient 2 in the finite-field/Schubert polynomial is therefore a Schubert-cell/ruling multiplicity;
- it is not the Hodge-star `+/-` eigenspace decomposition.

This correction matters for the RH/Grassmannian front because the elementary point-count combinatorics can be formalized without importing a false SD/ASD interpretation.

The elementary polynomial identity to formalize is

`[4 choose 2]_q = 1 + q + 2 q^2 + q^3 + q^4`,

or equivalently

`(q^4-1)(q^3-1) = (q^2-1)(q-1)(1+q+2q^2+q^3+q^4)`.

That core is independent of Deligne/Hasse–Weil theory. The remaining deep gap is the local/global zeta packaging.

## 4. Majorana/neutrino correction survives as a no-go theorem

v13 converts an earlier prediction into a valid obstruction.

For a 2+1 involution split and a symmetric mass matrix satisfying a shadow-odd relation, the matrix is forced to off-diagonal block form

`M = [[0_{2x2}, b], [b^T, 0]]`.

Direct linear algebra gives singular values

`(||b||, ||b||, 0)`.

This is independently checkable from `M†M`: its nonzero eigenvalue `||b||^2` has multiplicity two. Hence the exact low-energy symmetry forces one zero singular value and a degenerate massive pair.

The surviving conclusion is therefore a no-go statement: exact low-energy shadow-odd Majorana symmetry of this form cannot reproduce two distinct nonzero neutrino mass-squared splittings. Any viable physical model must break the symmetry, move it upstream (e.g. seesaw level), or alter the left/right action.

This is a high-value formal target because it replaces several old `True := trivial` prediction scaffolds with a precise finite-dimensional theorem.

## 5. Spectral-weight chronology and the clean scale-invariant cut locus

Source `spectral_weight_v2.pdf` (Drive file `19z5GndVfCeRaXAG1hRtBHxdgrffSwori`) explicitly supersedes `haar_qg_paper_v2.1.5.1`.

The surviving cut formula is

`Phi(Delta5,Delta6;M) = (1/(8*pi)) (M/2)^(Delta5+Delta6-2) Gamma(Delta5)Gamma(Delta6)/Gamma(Delta5+Delta6)`.

Hence `Delta5+Delta6=2` is the zero-homogeneity locus where the explicit M-scale disappears. No pole localizes the answer there and no residue is taken.

On `Delta5=1+i lambda`, `Delta6=1-i lambda`, the derived weight is

`P(lambda)=Gamma(1+i lambda)Gamma(1-i lambda)=pi lambda/sinh(pi lambda)`.

The archive explicitly retracts the earlier claims that P is the scalar `SL(2,C)` Plancherel density, that it replaces the Feynman loop measure, or that it makes quantum-gravity loops UV finite without counterterms.

What survives is exact special-function structure, including the Fourier pair with `(1/4) sech^2(x/2)` and the exact first moment `M1=1/8`. The `M2=1/90` statement is numerically verified in the paper rather than proved symbolically there, so Verify2 should preserve that status distinction unless an exact proof is supplied.

## 6. Arithmetic wave-particle interpretation: current safe formulation

The archive supports a precise but limited use of the phrase "arithmetic wave-particle duality":

1. atomic/discrete side: prime powers with von Mangoldt weights;
2. oscillatory side: Dirichlet/Fourier/Poisson modes in logarithmic coordinate;
3. diffusive side: causal heat-boundary response / Gaussian transforms of the same prime-power support;
4. Mellin inversion supplies the natural multiplicative spectral coordinate and reflection structure.

This is a family of exact transforms/representations of arithmetic data, not a new physical particle ontology.

The unresolved RH step is still global: construct the completed prime+Archimedean object with the required cancellation and positivity/spectral realization. Neither a local heat identity, a finite-prime positive kernel, nor the Mellin reflection alone proves RH.

## 7. U(1), SU(2), SU(3) and literal SU(1)

No credible archive layer produces a nontrivial `SU(1)`. It is trivial.

The safe Standard Model labels are `U(1)`, `SU(2)`, `SU(3)`. Separately, the octonionic stabilizer chain in the later corrected material is

`G2 superset SU(3) superset S(U(2) x U(1))`.

That nested chain is not itself a derivation of the direct-product Standard Model gauge group. The archive explicitly demotes any such claim.

Likewise, Spin(8) triality is exact representation theory but does not by itself derive three fermion generations.

## 8. Verify2 audit at current main

Current `GPPVerify2/main` observed during this pass: `6466bab739a5a4a262e70e4422c74aa0b095a508` (`Correct Grassmannian Haar provenance`).

`GppVerify/CelestialHolography/HolographicChain.lean` still contains two content-free stubs:

- `hasse_weil_gr24_factorization : True := trivial`;
- `bost_connes_celestial_restriction : True := trivial`.

The second is superseded by v13 and should be retired by deletion, not "proved". The later archive explicitly places Bost–Connes/Riemann-zero extensions outside the proof spine and requires any future bridge to start from the genuine arithmetic Bost–Connes representation rather than a declared celestial prime-sublattice restriction.

The first should be decomposed rather than deleted:

- formalize the Gaussian-binomial/Schubert point-count polynomial now;
- leave only the actual arithmetic-geometric local/global zeta construction as the deep remaining gap.

A further provenance issue in the same file is `three_constants_count : (3 : Nat)=3 := rfl` with prose claiming "Exactly 3 fundamental constants from 3 Cayley–Dickson doublings". The Lean theorem proves only the tautological count `3=3`; it does not prove the physical dictionary `(hbar,c,G) <-> three doublings`. v13 explicitly labels that dictionary conjectural. Documentation should be corrected without pretending the theorem proves physics.

## 9. High-value next formal targets from this mining pass

1. Universal weighted inversion `J_d`, half-density conjugation to Haar inversion, and Mellin reflection `z -> d-z`.
2. Exact doubling intertwiner `D(z)=2z` between d and 2d reflection laws.
3. `Gr(2,4)` Gaussian-binomial/Schubert polynomial and the coefficient-2 middle-cell multiplicity, with no SD/ASD identification.
4. Finite-dimensional shadow-odd Majorana no-go theorem yielding Takagi/singular spectrum `(m,m,0)`.
5. Correct pseudo-Hermitian `(2,2)` Grassmannian real structure / maximal-isotropic fixed locus, separated from the positive-definite complement.
6. Operator-level causal heat / prime-power response and the global prime–Archimedean completion, preserving the exact RH gap.
7. Continue axiom/stub retirement by deleting superseded targets and splitting large deep stubs into elementary proved cores plus honest residual gaps.

## 10. Promotion rules from this pass

Promote exact algebra, finite-dimensional linear algebra, measure/Jacobian identities, Mellin identities, and clearly cited established theorems.

Do not promote numerical agreement as proof; do not equate shadow with Wigner T; do not identify Schubert multiplicity with Hodge eigenspaces; do not claim triality derives generations; do not claim Haar self-duality alone creates the critical line; do not claim the spectral weight P is a Plancherel or loop measure; do not claim the Bost–Connes system is a celestial restriction; do not label a tautological Lean count as a physical derivation.
