# Codex/GPT run 11: dominated differentiation and frontier re-audit

Date: 2026-09-08
Owner: Codex/GPT only

## Continuous chamber / Wiener–Hopf line

GPPVerify2 commit `438fad4a6304dd35305a42d062998e9f9f2fe715` adds the actual dominated parametric-integral theorem for the compensated sech Lévy kernel.  For `c >= 0`,

\[
\frac{d}{dt}\int_{\mathbb R} (1-\cos(tx))\,\nu_c(x)\,dx
=\int_{\mathbb R} x\sin(tx)\,\nu_c(x)\,dx,
\qquad
\nu_c(x)=\frac{c}{|x|\sinh(\pi|x|)}
\]

in the precise `HasDerivAt` sense.  The local-uniform domination uses the already-certified Cauchy majorant on the unit ball around `t`, with the fixed window `T=|t|+1`.  Full Verify2 Build #2120 passed, including source/build-log sorry gates, axiom audit, and stub census.

The remaining analytic closure is therefore not differentiation under the integral.  It is the exact transform

\[
\int_0^\infty \frac{\sin(tx)}{\sinh(\pi x)}\,dx
=\frac12\tanh(t/2),
\]

followed by integration in `t` and the normalization at `t=0`.

## Yang–Mills / generalized-cut frontier correction

A current-branch audit shows that the four-point adjacent-MHV massive-scalar rational sector is substantially farther advanced than the recent rotation summaries implied.

Existing exact scripts already establish, in Badger's `s23` double-cut variables:

1. the pure `mu^2` bubble boundary vanishes;
2. the surviving triangle subtraction is generated by the quadratic uncut propagator pole;
3. the two subtraction roots are summed branch-free;
4. the exact `T1,T2,T3` moments yield one scalar-flow contribution
   \[
   -\frac{i(5u^2+3)}{3(1+u^2)};
   \]
5. complex-scalar multiplicity doubles this to the invariant coefficient
   \[
   C_{2;23}^{[2]}(--++)
   =\frac{2i}{3}\frac{2s_{12}-3s_{23}}{s_{23}^2}Q;
   \]
6. the FDH rational remainder then collapses algebraically to
   \[
   R_4^{\rm FDH}(--++)=-\frac{2i}{9}\frac{s_{12}}{s_{23}}Q.
   \]

The exact final cancellation is already formalized in Verify2 (`FourPointMHVRationalClosure.lean` / `FourPointMHVRationalAlgebra.lean`) conditional only on the amplitude-specific coefficient relation where appropriate.

Separately, `extract_generic_ds4_baseline.py` already gives exact stripped generic nonzero-`mu` massive-vector-minus-scalar sewings:

\[
C^{(4)}_{\rm same}
=\frac{2\rho^4+16\beta^2}{(1-\beta\cos\theta)^2},
\]

and, with `u=beta^2 sin^2(theta)`,

\[
C^{(4)}_{\rm mixed}
=\frac{2(u^2-8u+8)}{(1-\beta\cos\theta)^2}.
\]

These are stripped state-sum identities only: overall coupling/color normalization, cut orientation, loop-measure normalization, and topology-specific subtraction remain external before calling them complete physical master coefficients.

Therefore the honest one-loop YM frontier is no longer construction of the generic `D_s=4` baseline itself.  It is the convention-locked insertion of these generic sewings into the physical cut/topology projection with all normalization and subtraction data explicit.  Gravity double-copy numerators and higher-loop generalized cuts remain downstream of that normalization/topology closure.

## Prime-gas geometry

The executable two-parameter zeta-Gibbs curvature probe already samples both positive and negative scalar curvature.  Thus a conjectural global statement `R <= 0` is not merely unproved; it is numerically contradicted by the current discovery model.  The strongest certified global inequality remains

\[
R(\beta,\eta)<\frac12,\qquad \eta>0.
\]

## Completed-zeta / Weil boundary

No arithmetic promotion occurred.  The positive-real half-density, `Delta=2s`, critical-line unitarity, Gamma/Mehler–Fock, Wiener–Hopf, and continuous chamber identities remain local/Archimedean structure.  The missing global theorem is still positivity/factorization of the completed prime-plus-Archimedean Weil quadratic form (equivalently the unresolved full arithmetic coupling), so no RH claim is promoted.

No Claude-owned branch, note, file, or record was inspected.
