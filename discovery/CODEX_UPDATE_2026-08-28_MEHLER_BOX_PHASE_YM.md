# Codex/GPT cross-front update — 2026-08-28

Scope: Codex/GPT track only. No Claude material inspected.

## Spectral / Mehler–Fock / Wiener–Hopf

The dedicated sech lane at Verify2 head `290d0ba4a925b5d4adbef456c279f85699c7f03a` failed downstream of the already-successful all-order chamber product. The concrete failures in `SpectralRhoMehlerFockBridge.lean` were elementary normalization/coercion goals, not a mathematical obstruction.

Repair pushed as Verify2 `60b3560efddca6ebe77ed87a37c8cca0536992b8`: the real normalization is closed by `field_simp` + `ring`, and the real identity is transported to `ℂ` by `congrArg (fun r : ℝ => (r : ℂ))` rather than brittle `exact_mod_cast`.

The exact chamber product remains

`rhoGamma k λ = chamberCoeff k * chamberPoly k λ * rhoGamma 0 λ`,

with `chamberCoeff k = 2^(2k)/(2k+1)!` and `chamberPoly k λ = ∏_{j=1}^k (j^2+λ^2)`.

The base scalar-cut/Wiener–Hopf normalization is propagated through the full chamber hierarchy in Verify2 `87775586c9e5e6a504ec7e8c620afc561d25dcda`:

`rhoGamma k λ = chamberCoeff k * chamberPoly k λ * (2/π) * extendedWienerHopfWeight λ`.

This is an exact algebraic spectral-weight bridge. It is not yet a theorem that the chamber hierarchy is generated by iterated convolution.

## Principal series / completed zeta

Verify2 `6c97d71dc1330ed65b828e811d2eb3fbec212ccd` adds conjugation oddness for the real phase-response normalization on `Re Δ = 1`:

`celestialCompletedPhaseResponse Δ = -celestialCompletedPhaseResponse (conj Δ)`

away from completed-zeta zeros and the excluded pole points. Together with the existing imaginary-part-zero theorem, the phase response is real on the principal axis and odd under the shadow/conjugation involution. This is functional-equation/unitarity structure only and gives no zero-location theorem.

## Raised scalar box regulator

The raised-box files were confirmed present in the current workbench. Existing layers prove the one-channel pointwise majorant and the Beta convergence factors. Verify2 `8ee7320edd6330c57d3ab88febf9a58ac2054221` adds the exact affine simplex-slice identity

`∫_0^(1-x) y^(-δ) ((1-x)-y) dy = (1-x)^(2-δ) B(1-δ,2)`

in Mathlib's complex-power formulation, for `x < 1`.

The remaining analytic closure is to identify the full nested three-simplex majorant integral by Fubini/AE endpoint handling, obtaining the second factor `B(1-δ,3-δ)`, and then apply dominated convergence to the raised-box regulator limit.

## Prime-gas thermodynamics

No retraction. The Gibbs differential lane was green at the preceding tested head. The exact laws retained are

`C = β² κ₂`, `S' = -β κ₂`, `F'' = -κ₂/β - 2S/β³`, and `κ₂'' = κ₄ > 0` on `β > 1`.

The useful next asymptotic frontier remains a controlled `β → 1+` renormalized Fisher/fluctuation limit.

## Yang–Mills / gravity

`MassiveVectorWardReconstruction.lean` was re-audited. It already proves the correct five-dimensional Ward reconstruction `p·J₄ = κ J₅`, the equality of the longitudinal massive-vector projector term with the fifth-current product, the two-projector reconstruction, and the exact three terms omitted by replacing two massive projectors by bare four-dimensional metric contractions.

Therefore the honest Yang–Mills blocker has been sharpened: projector bookkeeping is not the missing step. The missing step is the explicit relevant tree current/current components for the `D_s=4`, `μ≠0` cut and their actual contraction into the sewn numerator. Gravity/double-copy and generic generalized-cut promotion remain downstream of that numerator calculation.

## CI

At latest Verify2 head `8ee7320edd6330c57d3ab88febf9a58ac2054221`, five PR workflows instantiated. The dedicated sech lane was still running and had passed setup/cache but had not yet reached the repaired Mehler–Fock bridge at the last check. No new green claim is made until that job completes.
