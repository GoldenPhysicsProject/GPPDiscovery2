# Codex run: all-order prime Hankel and spectral-rho promotion

Date: 2026-08-31
Branch: `codex/discovery-workbench`

## Verified-source promotion

The current Verify2 workbench already contains two stronger exact theorem lanes that were not imported by `GppVerify/FullConstruction.lean`. They have now been promoted into the common construction graph in Verify2 commit `9881d7274a7d48237f917808d978d6dddd5c2a47`.

### Prime-gas all-order strict Hankel geometry

`GppVerify.RiemannHypothesis.PrimeHankelAllOrderStrict` proves, for every `β > 1` and every nonzero real polynomial `p`,

\[
0 < \sum_{n\ge 0} \Lambda(n)\log n\,e^{-\beta\log n}\,[p(\log n)]^2.
\]

The strictness witness is completely arithmetic and uniform in polynomial degree: the first `N+1` positive powers of two have positive von-Mangoldt/Fisher weight and distinct logarithmic support values `(k+1) log 2`; a nonzero degree-`N` polynomial cannot vanish at all of them. Polynomial-square summability supplies the infinite lift.

This is stronger than the previously integrated 2x2 Fisher determinant. It gives strict positive definiteness of every finite Hankel/moment Gram sector generated by polynomial observables in `log n`, for the actual prime-Fisher weight, throughout `β>1`.

Important boundary: this is an arithmetic fluctuation/Hankel positivity theorem. It is not by itself the Weil explicit-formula positivity criterion and does not imply RH.

### All-order normalized Gamma / Mehler-Fock chamber hierarchy

`GppVerify.QuantumGravity.SpectralRhoChamberProduct` proves for all `k >= 0`

\[
\rho_k(x)=\frac{2^{2k}}{(2k+1)!}
\prod_{j=1}^{k}(j^2+x^2)\,\rho_0(x),
\]

with the chamber polynomial explicitly nonnegative and even. `SpectralRhoMehlerFockBridge` then identifies the base family and all chambers with the explicit Mehler-Fock form, including the removable value at `x=0`. Away from zero,

\[
\rho_k(x)=
\frac{2^{2k+1}x}{(2k+1)!\sinh(\pi x)}
\prod_{j=1}^{k}(j^2+x^2),
\]

and at zero the exact filled value is

\[
\rho_k(0)=
\frac{2^{2k+1}(k!)^2}{(2k+1)!\pi}.
\]

This is an exact all-order Gamma-family chamber theorem. It deliberately uses no Fourier transform, convolution theorem, or normalization integral, so it must not be reinterpreted as proof that higher loop chambers are repeated sech convolutions.

## Source-mining reconciliation

The focused `kinematic_block_v1-1.tex` source identifies the spectral weight `P(λ)=πλ/sinh(πλ)=|Γ(2s)|²`, Mehler-Fock structure, digamma critical-line density, and Wiener-Hopf factors as the Archimedean side of the intended explicit-formula program. It explicitly leaves the manifest-square representation and the multiplicative prime interference/global Weil step open. This agrees with the current formal boundary and supports keeping the all-order Gamma chamber algebra separate from any RH claim.

The focused `Loops_from_Cuts_in_Celestial_Holography.tex` source independently fixes the amplitude purpose of the same spectral factor: it derives `P(λ)` from the shadow-complete two-particle phase-space cut and gives the exact cut -> Mellin -> regulated box -> dispersion chain. That source therefore ties the spectral hierarchy back into the active scalar-box/celestial cut program without needing a speculative all-loop convolution interpretation.

## Scalar-box / amplitude frontier

No new scalar DCT theorem was claimed in this run. The real one-dimensional endpoint, inner slice, middle affine integral, and outer domination are already formalized. The remaining analytic theorem is still the nested real `L¹` assembly plus almost-everywhere removal of boundary faces and dominated convergence for the actual affine-simplex moment, yielding

\[
J_\varepsilon(S,T)\to \frac16.
\]

After that, the next physical amplitude target remains the fixed-loop-momentum, nonzero-`μ` Yang-Mills tree sewing numerator. Existing massive-vector state-count/projector algebra is infrastructure, not a substitute for the actual sewn tree numerator. Higher-loop/generalized cuts and gravity remain downstream.

## Principal-series / Weil frontier

The `Δ=2s`, positive-real half-density, critical-line unitarity, completed-zeta response, Gamma/Mehler-Fock, and Wiener-Hopf structures are mutually consistent and increasingly import-connected. The missing global step remains identification of a prime-plus-Archimedean trace/Gram functional with the genuine Weil quadratic form on an adequate test class and an unconditional positivity proof. No local spectral positivity result is being promoted across that boundary.

## CI state

Verify2 commit `9881d7274a7d48237f917808d978d6dddd5c2a47` triggered 24 workflows. At the first post-push audit there was no reported failure; the axiom/scaffold audit had already completed successfully while other lanes were queued/running. Treat the integration head as under CI until the full construction and ordinary build settle.
