# Codex/GPT all-fronts continuation — 2026-09-04

Codex/GPT track only. No Claude-owned branch, notes, files, records, or workspace were inspected.

## 1. Prime-gas thermodynamics / fluctuation geometry

Verify2 cold changed-Lean #893 passed on commit `0ca04b9af73830a9865a20c84d3f235bf902e71c`, closing the source gate for the Massieu response/Hessian wrapper. The exact entries are

\[
F_{\beta\beta}=\frac{M_2 Z-M_1^2}{Z^2},\qquad
F_{\beta\eta}=\frac{M_3 Z-M_1M_2}{Z^2},\qquad
F_{\eta\eta}=\frac{M_4 Z-M_2^2}{Z^2}.
\]

Together with the already-certified countable derivatives, these are the exact second responses of `log Z` through the negative derivatives of the normalized sufficient-statistic expectations.

A new Verify2 module, `NumberGibbsQuadraticMassieuFisherIdentification.lean`, was pushed at `55c191ff5ba858b49b40bb30429dc2fd8cf6a3c5`. It targets the exact identity

\[
F_{\beta\beta}F_{\eta\eta}-F_{\beta\eta}^2
=
\operatorname{fisherDet}\!\left(\frac{M_1}{Z},\frac{M_2}{Z},\frac{M_3}{Z},\frac{M_4}{Z}\right),
\]

then transfers the already-proved strict positivity of the normalized Fisher determinant for `eta > 0`. Cold changed-Lean #894 is pending/running at the time of this record; do not treat this new determinant transfer as certified until that gate is green.

## 2. Nonzero-mu Yang-Mills / generalized cuts

The generic 5D-null color-ordered tree uses adjacent cubic propagators `p12=q1+k2` and `p23=k2+k3`. Exact symbolic reduction gives

\[
 p_{12}^2=-2E^2(1-\beta\cos\theta),\qquad p_{23}^2=4E^2.
\]

Thus for

\[
 x=1-\beta\cos\theta
   =\frac{2(r^2+t^2)}{(1+r^2)(1+t^2)},
\]

`x` is exactly the unique angle-dependent adjacent tree-propagator coordinate in this ordering. Consequently the previously derived `x^{-2}`, `x^{-1}`, and polynomial sectors of the sewn `D_s=4` state sum have a precise diagram-level ancestry: double, single, and zero occurrences of the `p12` propagator across the two tree factors.

This is not yet a box/triangle/bubble master-integral assignment. Badger-style generalized-cut subtraction / integrand reduction is still required before identifying those Laurent sectors with master coefficients.

A new exact regression script `generic_tree_propagator_residue_audit.py` was added and gated in the generic `D_s=4` workflow. CI #11 initially failed only because a SymPy structural-equality assertion rejected algebraically identical numerator trees. The assertion was replaced by an algebraic zero test with no formula change. Discovery CI #12 then passed on `c4ebacc460707f98b13680322a68fba1ac057f78`, together with the generic baseline, topology-obstruction audit, and Ward-identity audit.

## 3. Scalar box / regulator

No regression. The cold-certified raised-box endpoint remains

\[
J_\varepsilon(S,T)\longrightarrow \frac16
\]

on the established physical domain. The scalar analytic front is closed; the amplitude frontier is topology extraction from the genuine nonzero-`mu` sewing.

## 4. Positive-real principal series / completed zeta / Weil

No RH promotion. The repository still contains an unconditional finite-support equivalence between critical-line fixation and positivity of the zeta-involution paired form, but the analytic step that would prove the genuine completed prime-plus-Archimedean Weil form positive remains absent. The exact blocker remains unconditional arithmetic positivity on an adequate admissible class, together with the necessary explicit-formula/multiplier/interpolation closure.

## 5. Gamma spectral weight / Wiener-Hopf / Mehler-Fock / chamber convolution

The existing Lean `SpectralWeightBetaBridge.lean` already proves globally for the continuous extension

\[
P_{\rm cont}(\lambda)
=B(1+i\lambda,1-i\lambda),
\]

including the removable point `lambda=0`. This confirms that the next high-value formal step is not another Gamma-product lemma but the real-line logistic change of variables in the Beta integral, followed by Fourier normalization. The arbitrary-`c` target remains

\[
\rho_c(x)=\frac{2^{2c-1}}{\pi\Gamma(2c)}|\Gamma(c+ix)|^2,
\qquad
\widehat{\rho_c}(t)=\operatorname{sech}^{2c}(t/2),
\]

with `rho_c * rho_d = rho_{c+d}` by Fourier uniqueness. No unsupported Barnes axiom or `SL(2,C)` Plancherel identification is introduced.

## Next frontier

1. Terminal cold gate for Verify2 determinant/Fisher identification (#894).
2. On YM, derive the actual generalized-cut projector/master subtraction from the exact `p12` propagator ancestry and existing Badger triangle/bubble machinery.
3. On the spectral side, formalize the Beta-to-logistic real-line integral before the full arbitrary-`c` Fourier theorem.
4. On RH, continue only through genuine explicit-formula/Weil positivity bridges; no local positivity result is to be promoted into RH.
