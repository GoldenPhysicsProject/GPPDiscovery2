# Codex/GPT run update — 2026-08-28 06:27 UTC

Codex/GPT track only. No Claude work inspected.

## 1. Spectral Gamma / Mehler–Fock / Wiener–Hopf front

The Gamma-pair recurrence now compiles cleanly without `sorryAx`:

\[
\Gamma(a+1+ix)\Gamma(a+1-ix)
=(a^2+x^2)\Gamma(a+ix)\Gamma(a-ix),\qquad a>0.
\]

The remaining CI failure had moved into the normalized family

\[
\rho_k(x)=\frac{2^{2k+1}}{\pi(2k+1)!}
\Gamma(k+1+ix)\Gamma(k+1-ix).
\]

The exact recurrence is

\[
\rho_{k+1}(x)=
\frac{2((k+1)^2+x^2)}{(k+1)(2k+3)}\rho_k(x).
\]

Its multiplier has the sharp chamber threshold

\[
R_k(x)>1 \iff 2x^2>k+1,\qquad
R_k(x)=1 \iff 2x^2=k+1,\qquad
R_k(x)<1 \iff 2x^2<k+1.
\]

Thus each successive normalized Gamma chamber suppresses the central spectral region and amplifies the tail beyond

\[
|x|>\sqrt{(k+1)/2}.
\]

Lean repair commit on `codex/lean-workbench`: `20c39f5059d4132c9ff6479bc16ecae691cfdd6c`.
The run for that exact commit was cancelled only because the shared Codex branch advanced immediately afterward; no mathematical failure was reported for this patch before cancellation. The current branch head is under fresh CI.

## 2. Grassmannian mass/orientation front

The exact opposite-chart fourth-power result has now been formalized on the shared Codex branch in `GrassmannianChartJacobian.lean`:

\[
J_{\rm scaled}^4=D^4 I,
\]

hence for the actual Jacobian on the big-cell overlap, when `D\neq0`,

\[
J_F^4=D^{-4}I.
\]

This explicitly retracts the old stronger `J^2=-I` statement at `D=1`.
The theorem is denominator-free at the polynomial level and is now separately CI-gated.

## 3. Scalar-box front audit

The regulator-limit architecture is substantially formalized. `ScalarBoxPhysicalConvergence.lean` proves the final one-sided regulator convergence once the moving physical core obeys the explicit `physicalCoreMajorant` bound. All subsequent analytic squeeze/prefactor steps are discharged.

Therefore the honest remaining scalar-box analytic joint is not the abstract limit machinery: it is the physical pointwise/eventual estimate

\[
|D(m)-D_0(m)|\le M_{\rm phys}(m)
\]

for the actual moving core, together with connecting that core to the dispersion/cut representation used in the physics normalization.

## 4. Yang–Mills / gravity numerator front audit

`MassiveVectorStateSumReconstruction.lean` deliberately stops short of claiming the missing physics. It formalizes dimensional state bookkeeping only and explicitly records that it does **not** compute the still-missing `D_s=4`, `\mu\neq0` gluon sewing numerator. That remains the honest YM cut frontier. State counting or dimensional reconstruction cannot substitute for this numerator.

The branch already contains separate MHV/rational, `mu^2`, `mu^4`, gravity `mu^8`, Ward-reconstruction, and tree-loop sewing modules; their existence does not close the missing baseline numerator unless the actual sewn tree product is established.

## 5. Prime-gas thermodynamics audit

The current Codex branch already contains exact real-axis Gibbs geometry for `beta>1`:

- `Z(beta)=zeta(beta)` as the integer partition sum;
- normalized integer Gibbs weights;
- `U=-zeta'/zeta`, equal to the von Mangoldt L-series in `Re s>1`;
- `S=log Z+beta U`;
- the logarithmic second zeta response equals the Gibbs variance;
- the third logarithmic response equals the third central moment;
- variance and heat capacity are nonnegative;
- `dS/dbeta=-beta Var(log n)<=0`;
- `(dS/dbeta)^2=C Var(log n)`.

The next thermodynamic target should therefore be a genuinely new differential/free-energy identity or a controlled `beta -> 1+` asymptotic theorem, not re-proving normalization.

## Current frontier

1. Get the full Gamma chamber hierarchy CI gate green after the normalized-rho proof repair.
2. Close the actual physical scalar-box core estimate/dispersion identification rather than adding more abstract squeeze lemmas.
3. Compute the honest fixed-`mu`, `D_s=4` gluon cut numerator; only then propagate dimensional reconstruction and gravity squaring/state sums.
4. Extend prime-gas geometry at the differential/asymptotic level while keeping positivity confined to `beta>1`.
