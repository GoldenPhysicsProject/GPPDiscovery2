# Celestial homogeneity of the closed four-point MHV rational remainder

Codex/GPT discovery track, 2026-08-25.

The FDH four-point adjacent-MHV rational remainder has now reduced to

\[
R_4^{\rm FDH}(1^-,2^-,3^+,4^+)
=
-\frac{2i}{9}\frac{s_{12}}{s_{23}}
\frac{\langle12\rangle[34]}
     {[12]\langle34\rangle}.
\]

Write

\[
\Xi=-\frac{\langle12\rangle[34]}
          {[12]\langle34\rangle},
\qquad
\chi:=\frac{s_{12}}{s_{23}},
\]

so that

\[
R_4^{\rm FDH}=\frac{2i}{9}\,\chi\,\Xi.
\]

## Common momentum scaling

Under a common rescaling of all external momenta

\[
p_i\mapsto \Lambda p_i,
\]

Mandelstam invariants scale as

\[
s_{ij}\mapsto \Lambda^2 s_{ij},
\]

hence

\[
\frac{s_{12}}{s_{23}}
\mapsto
\frac{\Lambda^2s_{12}}{\Lambda^2s_{23}}
=rac{s_{12}}{s_{23}}.
\]

Spinors scale as `|i> -> sqrt(Lambda)|i>` and `|i] -> sqrt(Lambda)|i]`, so the helicity ratio `Xi` is also invariant. Therefore

\[
\boxed{
R_4^{\rm FDH}(\Lambda p_1,\ldots,\Lambda p_4)
=R_4^{\rm FDH}(p_1,\ldots,p_4).
}
\]

The rational remainder has overall momentum homogeneity degree zero.

## Celestial consequence

For massless celestial kinematics write

\[
p_i=\epsilon_i\omega_i q_i(z_i,\bar z_i).
\]

A common boost-energy scale `omega_i -> Lambda omega_i` therefore leaves the rational amplitude unchanged. Consequently its common-scale Mellin integral has exactly the same scale exponent as any other dimensionless four-point amplitude. On the principal-series parametrization

\[
\Delta_i=1+i\lambda_i,
\]

the common-scale integral therefore carries the standard boost-conservation support

\[
\boxed{\delta\!\left(\lambda_1+\lambda_2+\lambda_3+\lambda_4\right)}
\]

with no extra imaginary shift generated by the rational term.

This is the clean celestial distinction between the *internal* evanescent radial shells and the *external* Mellin homogeneity: the `mu^4` box and `mu^2` bubble pieces have different internal hyperbolic profiles, but after dimension shifting and summing their external four-point rational remainder is scale degree zero.

## Cross-ratio content

On four-point momentum-conservation support the dimensionless ratio

\[
\chi=s_{12}/s_{23}
\]

is determined by the real celestial four-point cross ratio (up to the conventional incoming/outgoing and cross-ratio definition). Therefore the rational remainder contributes no new independent energy scale: its nontrivial celestial data are an angular/cross-ratio factor `chi` times the fixed helicity phase `Xi`.

We deliberately do not fix `chi = +/- z` here because that sign/inversion depends on the precise all-outgoing and celestial-cross-ratio convention. The invariant statement `chi=s12/s23` is convention safe.

## Boundary

This note concerns the external Mellin homogeneity of the already integrated FDH rational remainder. It does not identify the regulator/transverse-mass radial coordinate `r` with the external boost scale and does not claim that the two internal shell profiles disappear before integration.
