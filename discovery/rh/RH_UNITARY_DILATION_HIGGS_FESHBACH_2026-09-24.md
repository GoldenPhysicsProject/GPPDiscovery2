# RH Higgs/Feshbach mechanism from the minimal unitary dilation of causal shifts
## Date: 2026-09-24
## Status: exact operator identities; RH not proved

This note identifies an exact Higgs/Feshbach structure behind the corrected causal Koszul boundary commutators.

The key observation is that the causal right shifts on H_L=L2(0,L) are compressions of the bilateral unitary translation group on L2(R). The prime-prime commutators that appeared in the corrected Hodge square are therefore precisely boundary-excursion terms created by compression. Restoring the missing dilation space removes those commutators and restores an exactly positive massive bulk.

## 1. Causal shifts as compressed unitary translations

Embed H_L into K=L2(R) as functions supported on [0,L], and let P be the orthogonal projection onto H_L, Q=I-P.

Let U_y be bilateral translation on K:
(U_y f)(x)=f(x-y).

For 0<=y<=L the causal shift is exactly

V_y=P U_y P |_{H_L}.

Because the U_y form a commuting unitary group,

U_a U_b^*=U_b^* U_a.

Compression destroys this double commutation. Directly,

[V_a,V_b^*]
=
-P U_a Q U_b^*P
+
P U_b^*Q U_aP.

Thus the entire causal commutator is a difference of two excursions through the boundary reservoir QK.

This is an exact identity.

## 2. Full-line prime Higgs operators

For each prime p<e^L put a_p=log p and q_p=p^{-s}. On K define

Ttilde_p=I-q_p U_{a_p}.

On H_L its compression is

T_p=P Ttilde_p P=I-q_p V_{a_p}.

On the full line all Ttilde_p commute with one another and with one another's adjoints, because they are functions of the same commuting unitary translation group.

Let eps_p,iota_p be the CAR creation/contraction operators on the finite prime Fock space and define the full-line supercharge

dtilde_s=sum_p Ttilde_p tensor eps_p,

Dtilde_s=dtilde_s+dtilde_s^*.

Then cross-prime CAR terms cancel exactly and normality gives

boxed:
Dtilde_s^2
=
[sum_p Ttilde_p^* Ttilde_p] tensor I_Fock.

Thus the unitary-dilated arithmetic Higgs bulk has an exact positive scalar Hodge mass.

On the critical line |q_p|=p^-1/2,

Ttilde_p^*Ttilde_p >= (1-p^-1/2)^2 I,

hence

Dtilde_s^2
>=
m(X) I,
m(X)=sum_{p<=X}(1-p^-1/2)^2
~ X/log X.

## 3. Exact Feshbach compression identity

Let P_F=P tensor I_Fock and Q_F=Q tensor I_Fock.

The compressed causal Hodge-Dirac is

D_s=P_F Dtilde_s P_F.

Therefore

D_s^2
=
P_F Dtilde_s P_F Dtilde_s P_F
=
P_F Dtilde_s^2 P_F
-
P_F Dtilde_s Q_F Dtilde_s P_F.

Since Dtilde_s is self-adjoint,

P_F Dtilde_s Q_F Dtilde_s P_F
=
C_s^* C_s,

where

C_s=Q_F Dtilde_s P_F.

Hence the exact identity

boxed:
D_s^2
=
P_F Dtilde_s^2 P_F
-
C_s^* C_s.

The causal boundary defect is therefore a negative Schur/Feshbach self-energy obtained by integrating out the missing unitary-dilation reservoir.

This is the precise operator origin of the prime-prime boundary commutator derived in the previous note.

## 4. Boundary commutators factor through the reservoir

Expanding C_s^*C_s reproduces both same-prime defect projections and the cross-prime terms. In particular, for p!=q,

[T_p,T_q^*]
=
-q_p conj(q_q) P U_{a_p} Q U_{a_q}^*P
+
q_p conj(q_q) P U_{a_q}^* Q U_{a_p}P.

Thus the previously mysterious connected prime-prime boundary interaction is not an arbitrary correction: it is generated by propagation into and back from the exact unitary boundary reservoir.

## 5. Higgs interpretation

The full-line system is the unbroken/unitary parent. Its Higgs mass is

M_s=sum_p |I-q_p U_{log p}|^2,

which is strictly positive and grows like X/log X on the critical half-density line.

Restricting to the causal interval removes boundary degrees of freedom. The missing degrees reappear as the Feshbach self-energy C_s^*C_s.

Thus a natural arithmetic Higgs mechanism is:

1. full-line/unitary arithmetic bulk carries the positive many-prime Higgs mass;
2. causal localization breaks the unitary translation symmetry and creates boundary Goldstone/defect channels;
3. the completed pole/co-Poisson/Archimedean boundary must supply the physical boundary reservoir;
4. once that reservoir is included, the apparent negative causal self-energy is absorbed into a larger positive/self-adjoint parent.

This is now an exact operator statement through step 2. Step 3 is the remaining arithmetic identification.

## 6. Off-critical displacement is literally a chiral Higgs mass after half-density normalization

Let A_t be a skew-adjoint first-order delay generator at critical half-density and form its self-adjoint chiral doubling

D_t =
[[0,-A_t],
 [A_t,0]].

Let

Gamma =
[[0,I],
 [I,0]].

Then Gamma=Gamma^*, Gamma^2=I and {D_t,Gamma}=0.

Moving off the principal-series line by sigma=Re(s)-1/2 replaces the chiral first-order operator by A_t+sigma I, whose Hermitianization is

D_{sigma,t}=D_t+sigma Gamma.

Therefore

boxed:
D_{sigma,t}^2=D_t^2+sigma^2 I.

So any zero mode of a fixed positive-metric completed delay-line Dirac would force sigma=0.

This is the exact Higgs mass mechanism for the critical line.

It does not yet prove RH because one must still prove that the completed zeta boundary determinant is the characteristic/relative determinant of this fixed sigma-independent self-adjoint delay-line system. That determinant identification is the remaining global theorem.

## 7. Relation to the existing global Schur determinant

The v34 construction already has an exact analytic bordered Schur determinant whose scalar response tends to Xi in the initial Dirichlet half-plane. It also already identifies the local holonomies p^{-s} with first-order delay propagation.

The new target is therefore sharper than 'find a Hilbert-Polya operator':

Convert the existing analytic Mobius-Archimedean Schur system into the minimal unitary dilation of the delay-Koszul bulk, with a fixed completed boundary condition.

If this succeeds, the off-critical coordinate sigma enters only as the anticommuting Higgs mass sigma Gamma. Then the identity

D_{sigma,t}^2=D_t^2+sigma^2

forces every completed zero mode to sigma=0.

## 8. Exact remaining theorem

Construct a fixed completed boundary reservoir B and a self-adjoint critical operator D_t on the unitary-dilated delay/Koszul space such that

(a) the boundary/Feshbach determinant equals Xi(1/2+sigma+it) up to a nowhere-zero factor;
(b) sigma enters only through D_t+sigma Gamma;
(c) Gamma is a self-adjoint involution anticommuting with D_t.

Then

Xi(1/2+sigma+it)=0
=> ker(D_t+sigma Gamma) nonzero
=> 0 in spectrum(D_t^2+sigma^2)
=> sigma=0.

That would prove RH.

The algebraic Higgs implication is exact. The only unproved part is the completed determinant/boundary identification.
